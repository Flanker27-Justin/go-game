# 五子棋 · 使用说明

一个五子棋小游戏，支持**双人对战 / 人机对战（三档 AI + 教育模式）/ 微信联机实时对战**，并预留了国际象棋、围棋、中国象棋的扩展接口。联机侧支持**一键邀请**（建房自动复制链接 + 大字房间号 + 二维码扫码入局）、**棋盘格数可选**（13/15/19，建房时选择，联机中房主随时可改=双方清空重开）、**联机公平猜先**（服务器掷硬币，赢家自选执黑/执白）、**断线自动重连**（刷新/断网 60 秒内自动回房并恢复局面）、**房间保留**（对方离开后可重新邀请原对方或新对手）、**心跳保活**、终局**再来一局**，以及**玩家栏 + 气泡聊天 + 语音/视频通话**（异地也能边下棋边交流）。另有**开局猜先**（抛硬币 / 猜奇偶决定谁执黑），配合 cpolar 公网隧道可异地朋友点链接即玩。

> **使用场景与最快上手**：本项目的主场景是**两个人异地各用一台设备对战**，
> 所以联机走的是「公网发布 + 链接入局」，**不需要两台设备在同一个 WiFi**。
>
> - **要联机（异地）** → 双击 `outputs/启动异地联机.bat`
>   （自动检查 Node 与 cpolar → 启动服务器 → 建立公网隧道 → 打印公网地址与二维码 → 用公网地址打开浏览器）
> - **只想自己玩** → 直接双击 `outputs/gomoku.html`（需同目录有 `engine/`），或 `node gomoku-server.js` 后访问本机地址
>
> 原理与完整流程见第三节。

---

## 一、文件清单

| 文件 | 作用 |
| --- | --- |
| `启动异地联机.bat` / `启动异地联机.ps1` | **异地联机一键启动（主入口）**：检查 Node → 检查 cpolar → 启动服务器（端口自动与隧道配置对齐）→ 建立公网隧道并抓取公网地址 → 验证公网可访问 → 打印地址 + **终端二维码** → 用公网地址打开浏览器 |
| `gomoku.html` | 游戏本体（页面与对局框架；**需与 `engine/` 子目录放在一起**，见下方说明） |
| `engine/gomoku-ai.js` | **AI 引擎（唯一权威实现）**：三档难度、VCF/VCT 连杀、双杀与双三阵法、棋型评分、Alpha-Beta 搜索、置换表、开局库。UMD 格式，浏览器下暴露为全局 `GomokuAI`，Node 下可直接 `require` |
| `engine/opening-book.json` | 开局库可读副本（由引擎模块内联使用） |
| `gomoku-server.js` | 在线对战服务器（原生 Node，无第三方依赖）：静态文件 + WebSocket 房间 + 服务端权威判胜 |
| `games/` | 棋类扩展骨架：国际象棋 / 围棋 / 中国象棋 / 适配器模板（开发中，缺省不影响五子棋） |
| 依赖（外部） | [Node.js](https://nodejs.org/)（必须）与 [cpolar](https://www.cpolar.com/)（异地联机必须） |

> **关于「是不是单文件」**：早期版本整页自包含，现已把 AI 引擎拆成独立模块
> `engine/gomoku-ai.js`（约 137KB），好处是页面从 275KB 降到 141KB、引擎可被 Node
> 测试直接 `require`、且只有一份权威实现不会两边漂移。**代价是不能只拷 `gomoku.html`
> 一个文件**——必须连同 `engine/` 目录一起带走，否则页面加载不到引擎、AI 相关功能全部失效
> （浏览器会直接报 `GomokuAI is not defined`）。

### 开发者工具（`work/` 目录）

| 文件 | 作用 |
| --- | --- |
| `work/build-engine.js` | 引擎模块生成器（幂等）：按段落标记重新拼装 `engine/gomoku-ai.js`，含自检（函数完整性/未定义引用/注释配平/语法校验） |
| `work/positions.json` + `positions-test.js` | **必杀题库**：25 个关键战术局面（挡活三/跳三/冲四、抢双三点、四三杀、VCF 防守、先手权、开局库），校验引擎是否给出可接受应手 |
| `work/bench.js` | 自对弈基准：先手胜率 / 平均手数 / 单步耗时分布，`--depth` 可输出决策层调用分布 |
| `work/matchup.js` | A/B 对弈：把两套引擎放进同一进程对抗，输出胜率、置信区间与「作白存活手数」 |
| `work/duel2.js` | **配对强度测试（推荐判据）**：黑方按预先录制的进攻序列落子，两套引擎在同一局面下各自执白，比较存活手数。哨兵特性：同一引擎差值恒为 0 |
| `work/verify-page-runtime.js` | 页面运行时验证：用 vm + DOM 桩真实执行拆分后的页面内联脚本（26 项） |
| `work/verify-engine-parity.js` | 与「拆分前冻结版本」逐局面比对引擎行为，确认改动没有引入回归 |
| `work/perf-hard-ai.js` | 困难档性能基准（19 路 AI 自对弈单步耗时） |
| `work/audit.js` | 引擎体检：分值表分档、跳型识别、决策层构成、缺失能力扫描 |
| `work/gen-opening-book2.js` | 开局库生成器 v2（复用引擎自身搜索，支持 `--depth/--stones`；单局面约 2.7s，全量生成耗时较长） |
| `work/qr.js` | 终端二维码生成器（零依赖，复用页面内嵌 QR 编码器）；命令行用法：`node work/qr.js "文本"` |
| `work/verify-qr.js` | 验证二维码真能被解码（用 jsqr 反解，避免“看着像二维码但扫不动”） |
| `work/check-doc.js` | 文档一致性检查：对照代码核对使用说明的表述（引擎拆分说明、断言数、工具清单、启动脚本编码等），避免文档过时误导 |
| `work/ws-test.js` | 服务器协议层自动化测试（87 项断言：建房/加入/落子/判胜/联机猜先/断线重连/再来一局/心跳/限频/房间回收/棋盘大小/昵称/通话信令/房间保留） |
| `work/online-e2e.js` | 联机端到端测试（真实浏览器：建房 → 扫码入局 → 对弈 → 刷新重连 → 胜负 → 再来一局 → 玩家栏/聊天/昵称/棋盘大小修改 → 语音通话 → 对方离开重邀 → 联机猜先，56 项断言） |

## 二、快速开始

### 方式一：异地联机一键启动（主推，两个人各在一地）

**双击 `outputs/启动异地联机.bat`**。它会：

1. 检查 Node.js（**没装会给出下载地址与 `winget` 安装命令**，不会一闪而过）；
2. 检查内网穿透工具 **cpolar**（异地联机的关键，未安装/未登录会给出具体命令）；
3. 启动游戏服务器——**端口自动从 cpolar 隧道配置里读取并对齐**（你的 `gomoku` 隧道绑定 8123，所以服务器也监听 8123）；
4. 建立公网隧道，并从 cpolar 输出中抓取**公网 https 地址**；
5. 用真实请求**确认公网地址可访问**；
6. 打印公网地址并**在终端打出二维码**（对方扫一下就能进）；
7. 用**公网地址**打开浏览器（这样复制出来的邀请链接才是公网的）。

进阶用法：

```powershell
# 指定端口 / 指定隧道名 / 不自动开浏览器
powershell -ExecutionPolicy Bypass -File 启动异地联机.ps1 -Port 8123 -Tunnel gomoku -NoBrowser
# 不用命名隧道，改走临时隧道（地址每次会变）
powershell -ExecutionPolicy Bypass -File 启动异地联机.ps1 -Tunnel ""
```

### 方式二：只玩单机

不需要联机时，**直接双击 `outputs/gomoku.html`** 即可（AI 与双人对战都能用，不需要 Node）。
但必须保证同目录下有 `engine/gomoku-ai.js`——浏览器在 `file://` 下会拒绝跨目录加载，
缺失时页面会报 `GomokuAI is not defined`。此方式下在线对战不可用。

### 方式三：手动启动（服务器 + 隧道分开跑）

```bash
node gomoku-server.js     # 在 outputs 目录执行（端口须与隧道配置一致，默认 8123）
cpolar start gomoku       # 另开一个窗口启动隧道；或 cpolar http 8123
```

然后**用 cpolar 打印的公网地址**在浏览器打开页面建房。完整流程见第三节。

### 一键启动的原理（出问题时按这张表排查）

| 做法 | 为什么必须这样 |
| --- | --- |
| 服务器绑定 `0.0.0.0` | `gomoku-server.js` 里是 `listen(PORT, '0.0.0.0')`，即监听本机**所有网卡**；绑成 `127.0.0.1` 时只有本机能连。 |
| **端口必须与隧道对齐** | cpolar 的 `gomoku` 隧道配置写死了 `addr: "8123"`，隧道会把公网请求转到这个端口。若服务器监听别的端口，公网地址就会打不开——脚本因此从 `cpolar.yml` 读端口。 |
| **必须做公网映射** | 你电脑的 `192.168.x.x` 只在自己路由器那张网里有效，异地对方访问它等于在他自己的网络里找一台不存在的机器。 |
| **必须用公网地址建房** | 邀请链接 = 当前页面地址 + `?room=房间号`。用 `127.0.0.1` 打开时生成的是本机链接，异地打不开。 |
| 二维码 | 手敲公网域名容易出错，扫码最省事。由 `work/qr.js` 生成：复用页面内嵌的 QR 编码器（**零依赖**），把矩阵渲染成半块字符（`▀▄█`）画到终端。 |
| `?room=XXXXX` 自动入局 | 建房后页面地址会带房间号，对方打开链接时页面读出该参数直接加入，**不用手输房间号**。 |
| 实时同步 | 浏览器与服务器之间是 **WebSocket 长连接**，cpolar 的 http 隧道会一并转发（已实测），落子即时双向推送。 |
| 服务端权威 | 是否轮到你、坐标是否合法、是否连五，全由服务器判定；改客户端也无法作弊。 |
| 语音/视频 | WebRTC 要求安全上下文，cpolar 给的是 **https**，正好满足。 |
| 免费版域名会变 | 每次重启隧道地址都会变，所以每次开局要把最新链接发给对方。 |

## 三、异地联机（两个人各在一地，主推场景）

本游戏的设计目标就是**两个人异地各用一台设备对战**，所以联机主线是「公网发布 + 链接入局」，
而不是局域网。**双击 `outputs/启动异地联机.bat`** 即可，它会：

1. 检查 Node.js；
2. 检查内网穿透工具 **cpolar**（异地联机的关键依赖，见下方"为什么必须用它"）；
3. 启动游戏服务器（端口自动与隧道配置对齐，见下）；
4. 建立公网隧道并**从 cpolar 输出里抓取公网地址**（优先 https）；
5. 用真实请求确认公网地址可访问；
6. 打印公网地址并**在终端打出二维码**（对方扫码即入）；
7. 用**公网地址**打开浏览器（这点很重要，见下）。
**关闭窗口即同时停止服务器与隧道。**

### 完整流程（两个人怎么配合）

| 谁 | 做什么 |
| --- | --- |
| 你 | 双击 `启动异地联机.bat`，等它打出「公网地址已就绪」 |
| 你 | 用脚本打开的**公网地址**进页面 →【在线对战】→ 选棋盘格数 →【创建房间】 |
| 你 | 页面会生成**房间二维码**并自动复制邀请链接；把这个二维码/链接发给对方（微信、QQ 都行） |
| 对方 | 点开链接或扫码 → 自动进入房间（**无需输入房间号**）→ 开始对弈 |
| 双方 | 可用【💬 聊天】交流；公网是 https，所以【📞 语音】【📹 视频】也能用 |

### 三个必须知道的点

1. **一定要用公网地址建房**。邀请链接是"当前页面地址 + `?room=房间号`"拼出来的。
   如果你用 `127.0.0.1` 打开页面，复制出的链接就是本机地址，**异地对方根本打不开**。脚本因此直接用公网地址开浏览器。
2. **端口要和隧道对齐**。你 cpolar 里那条名为 `gomoku` 的隧道配置写死了转发到本机 **8123**
   （`addr: "8123"`），所以游戏服务器也必须监听到 8123。脚本会自动从 `cpolar.yml` 读出这个端口并用它，
   避免"隧道把请求转到一个空端口、公网地址打不开"。
3. **免费版地址每次会变**。cpolar 免费套餐的公网域名在隧道重启后会变，所以每次开局都要把**最新链接**发给对方。
   想要固定域名需 cpolar 付费套餐。

### 为什么必须用 cpolar，以及它的原理

| 问题 | 解释 |
| --- | --- |
| 为什么不能用局域网 IP | 你电脑的 `192.168.x.x` 只在自家路由器这张网里有效。异地对方访问这个地址，等于在他自己的网络里找一个不存在的机器，**必然连不上**。 |
| cpolar 做了什么 | 在你的电脑和它的公网服务器之间建一条**反向隧道**：公网域名 `https://xxx.cpolar.top` 上收到的请求，会被转发到你本机的 8123 端口。对方访问这个域名 ≈ 访问你的电脑。 |
| WebSocket 也能过去吗 | 能。cpolar 的 http 隧道会一并转发 WebSocket 升级请求，所以实时落子同步不需要额外配置。本项目已实测：公网地址上建房 → 对方加入 → 双向落子同步 → 聊天转发，全部正常。 |
| 语音/视频为什么能用 | WebRTC 要求**安全上下文**，普通 http 页面拿不到麦克风/摄像头权限；cpolar 给的是 https，正好满足。信令仍走你自己的 WebSocket 服务器，媒体流经公共 STUN 打洞直连。 |
| 连不上怎么办 | ① 先确认窗口里第 ⑤ 步显示"公网地址可正常访问"；② 把链接重新发一次（免费版域名会变）；③ 极端对称 NAT 下 WebRTC 可能打不通，可自行配置 TURN 服务器（把地址加进 `gomoku.html` 顶部的 `ICE_SERVERS`）。 |

### 手动方式（不想用脚本时）

```bash
node gomoku-server.js          # 1) 在 outputs 目录启动服务器（必须监听隧道配置的那个端口，默认 8123）
cpolar start gomoku            # 2) 另开一个窗口启动隧道（或用 cpolar http 8123）
```
然后把 cpolar 打印的**公网地址**（形如 `https://xxx.cpolar.cn`）复制到浏览器打开 →
【在线对战】→【创建房间】→ 把页面生成的邀请链接发给对方。

> 若使用 cpolar 的 **TCP 隧道**而非 http 隧道，请在页面【服务器】输入框手动填写 `ws://xxx.cpolar.cn:端口` 形式的地址。
> 不想用 cpolar 时，也可以租一台公网云服务器，把 `gomoku.html`、`engine/`、`gomoku-server.js`、`games/`
> 上传后执行 `node gomoku-server.js`，并放行安全组 TCP 8123，效果相同。

## 四、同一 WiFi 下联机（可选，局域网）

只有当两台设备**在同一个路由器下**时才能用这种更简单的方式：

1. 在 `outputs` 目录启动服务器：`node gomoku-server.js`；
2. 查本机局域网 IP：命令提示符执行 `ipconfig`，取"无线局域网适配器"下的 IPv4（形如 `192.168.1.6`）；
3. **放行防火墙**（首次必须，否则对方打不开）：
   ```powershell
   netsh advfirewall firewall add rule name="Gomoku8123" dir=in action=allow protocol=TCP localport=8123
   ```
4. 让对方用 `http://你的IP:8123/` 打开页面，按第三节同样的方式建房/入局。

> 注意：这种方式**出了这个 WiFi 就失效**，而且没有 https，**语音/视频通话不可用**（浏览器限制）。
> 所以异地场景请用第三节的 cpolar 方案。

## 五、玩法与功能

- **三种模式**
  - 双人：同一屏幕轮流落子。
  - 人机：玩家与 AI 对弈（默认玩家执黑、AI 执白）。
  - 在线：通过邀请链接/房间号实时对战（见第三、四节）。
- **开局猜先（双人/人机/联机模式）**
  - 设置区"开局猜先"开关默认开启；每局开始前弹出小游戏决定谁执黑（黑棋先手）。
  - 双人/人机：**抛硬币**（正面=玩家1/你 赢，反面=玩家2/AI 赢）或**猜奇偶**（系统藏 1~5 颗棋子，猜中者赢）。
  - **联机猜先**：服务器权威掷硬币（客户端无法作弊），随机选一方先猜正/反面，猜中者赢；赢家自选执黑（先手）或执白，颜色与角色（房主/加入者）解耦——加入者也可能是黑棋。猜先中途刷新/断线会自动补发状态继续。
  - 赢家可自选**执黑（先手）**或**执白**；人机模式下玩家也可执白（AI 执黑先行），AI 难度与教育点评都会自动按新执子颜色工作。
  - 不想要这步可直接点【跳过】，默认玩家1/你（联机默认房主）执黑。
- **一键邀请（在线模式）**
  - 建房成功后**自动复制邀请链接**、大字显示房间号并生成**二维码**（内嵌编码器，零依赖）；对方打开链接或扫码即自动入局，无需输入房间号。
  - 邀请区（链接 + 复制 + 扫码）在**对局全程保留**：点【扫码入局】可随时展开/收起二维码，方便对方中途掉线后重新扫码进入。
  - 支持局域网 IP 与 cpolar 公网地址自动适配；也支持 `?invite=房间号` 两种参数写法。
- **玩家栏 + 气泡聊天（在线模式）**
  - 棋盘上方玩家栏实时展示双方：执子颜色、回合高亮（轮到谁谁亮起）、最近落子位置（第 N 手），一眼看清局面归属。
  - 【💬 聊天】默认折叠不占棋盘空间：收到新消息时按钮点亮并显示**未读角标**；气泡式消息带发送者昵称与时间戳；输入框上方有一排**快捷表情/常用语**（😄 👍 该你了 好棋！ 再来一局？…），点一下即发送。
  - **昵称**：入局后在"昵称"输入框输入并回车，双方玩家栏与聊天即时同步（≤12 字，随时可改）。
- **语音 / 视频通话（在线模式）**
  - 入局后点【📞 语音】或【📹 视频】发起通话，对方收到来电横幅可接听/拒绝；通话中可**静音、关摄像头、挂断**，30 秒无人接听自动取消。
  - 技术说明：基于浏览器原生 WebRTC + 公共 STUN 服务器做 P2P 直连，信令由本服务器转发，**零第三方依赖**。需要 **HTTPS 安全上下文**（cpolar 公网 https、或本机 `http://localhost`/`127.0.0.1`）；普通局域网 IP 的 http 访问受浏览器限制无法打开麦克风。跨严格 NAT 时若连不上，可自行配置 TURN 服务器。
- **联机中修改配置**
  - **棋盘大小**：**房主随时可改** 13/15/19（含对局中），改大小=双方棋盘清空重开一局（黑先），双方同步；加入者不可改，仅房主有权限。
  - **昵称**：双方随时可改并同步给对方（见上）。
- **对方离开后房间保留**
  - 对方（白棋）主动断开或重连超时，**房间与当前棋局保留**：房主收到提示，邀请区自动重新出现，把链接再发一次即可让原对方或新对手直接加入并恢复棋局。
  - 房主想清空棋局等待新对手，可点【断开】后重新建房；对方在线时【断开】会结束当前对局。
  - 对方刷新/断线 60 秒内算"暂时离开"：本端暂停落子并显示倒计时，对方自动重连后继续对局。
- **断线自动重连（在线模式）**
  - 建房/加入时下发重连凭证；**刷新页面或短暂断网不算退出**，页面自动按 1s→10s 指数退避重连，60 秒窗口内用服务端权威快照恢复棋盘/回合/胜负，双方继续对局。
  - 对方断线期间本地会暂停落子并提示"对方暂时离开，等待重连…（60 秒内自动恢复）"；重连成功提示"对方已重连"。
  - 超时未归才判定对方离开；点【断开】才是主动退出。服务器每 15s 心跳探活、45s 无响应回收僵尸连接，并限制单条消息 ≤8KB、每秒 ≤20 条防刷。
- **再来一局（在线模式）**
  - 对局结束后任一方点击【再来一局】，双方棋盘同步清零、**执子颜色不变**（沿用本局猜先结果或默认颜色），直接开下一盘，无需重新建房。
- **棋盘格数**：13 / 15 / 19 可选（默认 19），切换即新开一局；在线模式建房时选择、**房主随时可改**（改大小即双方清空重开，加入者无权限），双人/人机模式随时可切。
- **AI 三档难度**
  - 简单：单步启发式打分，出棋随机变化最大，适合新手；补上了最基本的"能赢就赢 / 对方要赢必须挡"。
  - 中等：棋型组合评分 + 一步必杀/挡杀检测 + VCF 冲四连杀 + VCT 活三链杀（自动选**手数最短**的杀棋路线）+ 对方双杀/活三链防守。
  - 困难：在中等基础上叠加 Alpha-Beta 剪枝搜索，并采用**迭代加深**（预算内自动从 3 层加深，中盘 5 层 / 残局 6 层），19 路棋盘单步最长约 2.5 秒（分层时间盒控制：每层分配预算，超时立即用已完成层的结果落子，不会长时间无响应），中后盘能看穿更长杀棋、更快兑现优势。
- **置换表 + 历史启发（困难档搜索加速）**
  - **Zobrist 置换表**：每个局面（含"轮到谁"）的搜索结果缓存复用，迭代加深各层与不同分支汇聚时不再重复展开同一局面，同一预算下能搜得更深；每次搜索开始重建哈希并清空缓存，结果完全确定、不受上一步影响。
  - **历史启发**：引发剪枝的"好手"按深度平方累加历史分，候选按历史分优先尝试，让剪枝更早发生；开局阶段历史分全为 0，保持原有启发式顺序，不影响开局套路与随机变化。
- **强制走法搜索（困难档核心提升）**
  - 搜索节点会先判断双方的"强制威胁"：对方可一手成活四/成五时必须先堵；自己能一手冲四/活四时只搜这些强制点。
  - 等于把"连续冲四杀棋"的思路融进博弈树：同一预算下分支数从十几个降到几个，能顺着杀棋链多看 2~3 层，残局对攻、快速兑现优势的能力明显更强。
  - 只把"一手形成双威胁"的冲四当强制手；单一冲四会被对方一手堵死，若强制只搜它会盲目冲四挥霍机会——普通冲四仍保留在候选里按启发式高分优先尝试。
- **双三"阵法杀招"（中盘转化优势的关键）**
  - 新增双威胁检测：一手能同时形成两个"活三以上"威胁（双三 / 活三+冲四 / 双四）时，只要对方没有更快威胁（不能一步成五、不能一手成活四），AI 会主动下出这个必胜阵型，把前中盘积累的优势直接兑现成杀棋。
  - 防守同等级的威胁时，优先选"挡住又能顺手形成双威胁"的落点；安静局面下布子优先做双威胁结构，而不是零散地凑单线。
  - 若对方已有活三等更快威胁，双杀不成立，AI 会先防守——不会为了贪阵法而漏防。
- **VCT 活三链杀（连续活三强制胜）**
  - 在双杀之后新增 VCT 搜索：AI 每一步都落成对方必须防守的活三/双三，模拟对方所有可能的堵法（AND 节点），全部堵不死才认定这条活三链必胜——这是中盘把优势转化为胜势的关键手段。
  - 对方存在活三链时，AI 会优先抢占对方活三/冲四的开放端点，选“堵住又能顺势反击”的点，而不是盲目进攻。
  - 10 层 / 5000 节点 / 300ms 预算双重上限，单步不会卡顿；命中时教学点评会提示“AI 发现了一条连续活三的杀棋路线（VCT）”。
- **先手权（tempo）评估**
  - 静态评估加入"轮到谁走"的修正：双方各有一个活三时，先手方下一手就能把活三变活四锁定胜局，评估不再误判成均势，AI 攻防取舍更准确。
- **防守权衡（不机械堵棋）**
  - AI 会区分“必须挡”和“可以放”的棋型：对方的**活三/活四/冲四**必须立即防守；但对方三连一端已被堵死（眠三）时，即使另一头落子能冲四，也只是一步步被挡、不构成杀棋，AI 会交给全局搜索/评分权衡，优先做能赢的棋，而不是处处设防。
  - 只在对方存在 VCF 连续冲四杀棋时，AI 才会强制堵冲四端点。
- **开局库（前 9 手定式）**
  - 内置由引擎自身搜索生成的**开局库**（覆盖 0~8 子局面、860 个规范化局面、2590 条应答，按 8 对称折叠：旋转/镜像同一开局命中同一表项）。
  - AI 前 9 手直接查库应手，响应极快、开局有章法；同一定式带权随机（困难档收敛、中等适度），配合上面的随机机制保证"同一开局不再永远同一手"。
  - 开局库同样服务于教育模式：推荐点与点评会给出库内定式应手。
- **AI 出棋变幻莫测**
  - 开局与普通选点会在得分最高的前几名里**带权随机**：同一开局不再永远同一手（难度越高变化越小，困难档只在两个同等级好点间浮动），强度几乎不受影响。
- **对手意图判断**
  - AI 会分析你每一步的目的（活四 / 冲四 / 活三 / 双杀陷阱 / 防守 / 布局）。
  - 发现你在酝酿双三/三四/双四时，会**抢先堵住关键点**让陷阱无法成形。
  - 教育模式下 AI 思路会明确说出来，如"AI 识破你的活三意图，先落子压制你的发展"。
- **教育模式（仅人机模式）**
  - 开启后，AI 会点评你每一步（成五 / 错过必杀 / 防守警告 / 双杀赞美 / 更优推荐 / 弱棋批评 / 棋型描述）。
  - AI 自己走出 VCF 冲四连杀 / VCT 活三链杀时，点评会明确说明这条杀棋路线，方便你学习如何把优势兑现成胜势。
  - "推荐点"开关：在棋盘上用绿色圆圈标出 AI 认为的最佳落点。
  - AI 落子后还会附带一句思路说明。
- **胜率预测（仅人机模式，可选开关）**
  - 设置区新增"胜率显示"开关（默认开启），开启后在状态栏下方显示"你 x% / AI y%"的渐变胜率条（绿色=你、红色=AI）。
  - 胜率由当前局面的静态评估分实时估算：空盘接近 50%，一方成活四/一步成五时直接锁定接近 100%。
  - 色条带 50% 白色中线：左侧绿色为你、右侧红色为 AI，谁占优一眼可辨；两侧百分比统一四舍五入到 0.1%。
  - 98% 封顶只对"轮到的一方"的一步成五生效：冲四这类对方下一步可挡的棋型不会误报成锁定胜局。
  - 双三/双威胁等"强而不必胜"的棋型在胜率估算中打折处理，只显示 60~80% 区间，避免三个连子就飙到 98% 的失真。
  - 关闭开关或切到双人/在线模式时自动隐藏；人机模式下开关不可用时为置灰状态。
- **悔棋 / 重新开始**：人机模式悔棋会同时撤回玩家与 AI 的两步；AI 思考中不可悔棋。

## 六、扩展新棋种（预留接口）

`games/` 目录已给出骨架。实现方式：

1. 仿照 `games/_adapter-template.js` 与 `gomoku.html` 末尾的 `gomokuAdapter`，实现同一套适配器接口（`id / name / modes / onEnter / onCanvasClick / onUndo / ...`）。
2. 在 `games/chess.js` 等文件中实现规则逻辑后，调用框架的 `registerGame(id, adapter)` 注册。
3. 在 `gomoku.html` 的游戏下拉框中即可切换。
4. 需要 AI 时，可参考 `engine/gomoku-ai.js` 的组织方式：做成 UMD 模块（浏览器挂全局、Node 可 `require`），
   对外暴露 `setBoardSize / setColors / getBestMove` 等接口，页面侧只负责调用，
   这样 AI 能被 Node 测试直接加载、也便于单独做基准与对弈评估。

## 七、AI 引擎与开发说明

**架构**：AI 引擎是独立模块 `engine/gomoku-ai.js`，是**唯一权威实现**；页面与全部测试都从它加载，
不再有第二份实现。页面里的 `const AI = GomokuAI` 就是它的引用。

**改引擎的流程**：

```bash
# 1) 直接改 outputs/engine/gomoku-ai.js（常量与函数都在这里）
# 2) 规范化并自检（幂等，会校验函数完整性/未定义引用/注释配平/语法）
node work/build-engine.js
# 3) 跑战术题库与断言（秒级，必须全绿）
node work/positions-test.js && node work/ai-threat-test.js && node work/ai-scoring-test.js
# 4) 若要评估棋力，用配对判据（见下）
node work/duel2.js --a work/archive/新引擎.js --b outputs/engine/gomoku-ai.js --positions 12
```

> **评估棋力必须用 `duel2.js`，不要只看 A/B 胜率。**
> 实测结论：当前引擎存在「执黑必胜」现象，谁执黑谁赢，因此 **A/B 胜率永远是 50%**，
> 无法区分强弱。`duel2.js` 让黑方按预先录制的进攻序列落子，两引擎在同一局面下各自
> 执白、比较存活手数——同一引擎的差值恒为 0（哨兵特性），是目前唯一有区分度的判据。

**已知的棋力瓶颈**（多次实测确认，避免重复踩坑）：

- 一整局中约 **83% 的着法由 getBestMove 的固定战术阶梯直接决定、不经过搜索**，
  因此单独修改搜索参数（宽度、半径、VCF/VCT 窗口、时间预算）或评估函数，
  **几乎不影响实际棋力**——实测 8 项此类改动（含加深搜索、必胜距离、滑动窗口评估）
  全部持平，多数局面决策完全相同。
- 真正要提升棋力属于**重构级**工作：需把 VCF/VCT/双杀的全盘扫描改成增量维护、
  把预算让给 minimax（目标 7~9 层稳定深度），并给叶节点评估提速
  （当前叶节点评估要上千次 `lineInfo`，是性能瓶颈的根源）。

## 八、跑一遍自动化测试

```bash
# —— 纯本地，不需要服务器 ——
node work/ai-threat-test.js       # AI 威胁/搜索层：73 项断言（require 引擎模块）
node work/ai-scoring-test.js      # AI 评估/胜率层：45 项断言
node work/positions-test.js       # 必杀题库：25 个关键战术局面
node work/verify-page-runtime.js  # 页面运行时：vm + DOM 桩执行页面脚本，26 项
node work/verify-engine-parity.js # 与拆分前冻结版本比对引擎行为（默认 30 局面 / 300 项）
node work/perf-hard-ai.js         # 困难档性能基准（单步耗时上限校验）

# —— 需要拉起服务器（会自行 spawn node 进程）——
node work/ws-test.js              # 协议层：87 项断言（联机猜先/断线重连/再来一局/心跳/限频/房间回收/…）
node work/online-e2e.js           # 真实浏览器端到端（需本机安装 Edge/Chrome）
```

说明：

- 两个 AI 断言测试与题库测试是**纯本地**的（引擎已独立成模块，直接 `require`），无需服务器；
  只有 `ws-test.js` 与 `online-e2e.js` 会拉起服务器做黑盒测试。
- 注意：`ws-test.js` / `online-e2e.js` 依赖 `spawn` 子进程与真实浏览器，
  在受限沙箱（禁止命名管道/子进程管道）或未安装浏览器的环境下会失败，这属于环境限制而非代码问题。
- 性能基准的验收口径是「单步不超过 3000ms」；题库的验收口径是「全部 25 个局面都给出可接受应手」。

## 九、常见问题

- **双击 `启动异地联机.bat` 一闪而过**：说明启动脚本自身报错了。请改为在 `outputs` 目录打开
  命令行手动执行 `powershell -ExecutionPolicy Bypass -File 启动异地联机.ps1`，即可看到完整报错。
- **提示"未检测到 Node.js"**：按脚本给出的提示安装（<https://nodejs.org/> 或
  `winget install OpenJS.NodeJS.LTS`），装完重新双击即可。
- **提示未找到 / 未登录 cpolar**：异地联机必须用它。安装后执行一次 `cpolar authtoken <你的token>`
  （token 在 cpolar 后台可查），再重新双击脚本。
- **公网地址打不开**：按顺序查——① 窗口里第 ⑤ 步是否显示"公网地址可正常访问"；
  ② 端口是否与隧道一致（脚本会自动从 `cpolar.yml` 读，手动跑时要自己保证，`gomoku` 隧道绑的是 8123）；
  ③ 隧道是否建起来（看 `logs/cpolar.log` 里有没有 `Tunnel established at ...`）；
  ④ 免费版域名重启后会变，把**最新链接**再发一次。
- **为什么邀请链接对方打不开**：多半是你用 `127.0.0.1` 打开的页面建的房，链接里带的是本机地址。
  请改用**公网地址**打开页面再建房（脚本会自动这么做）。
- **端口被占用**：脚本会优先对齐隧道配置的端口；若是被别的程序占用，会提示并尝试顺延。
  注意顺延后与命名隧道不再匹配，此时建议先释放 8123 或改用临时隧道（`-Tunnel ""`）。
- **只拷了 `gomoku.html`，页面打不开 AI 功能 / 报 `GomokuAI is not defined`**：
  引擎已拆成独立模块，必须把 `engine/` 目录一起拷贝到 `gomoku.html` 同目录下。
- **能打开页面但"正在连接服务器…"卡住**：检查服务器进程是否在运行、端口是否被占用，
  以及页面地址与 WebSocket 是否同源（用公网地址打开页面时才连公网 `/ws`）。
- **房间号输不进去**：只接受 5 位字母或数字，会自动过滤非法字符并转大写。
- **朋友打开邀请链接但没自动加入**：确认链接里的房间号没被微信截断（复制时完整选中）；若房间已结束或已满，重新建房再发一次；已加入过本房间的浏览器刷新时若提示"房间已满"，请点【断开】清除旧会话后再试。
- **刷新页面后对局没丢，但提示"重连超时"**：断线超过 60 秒窗口，房间已被回收，请让对方重新建房并发新链接。
- **点【复制链接】没反应**：部分手机浏览器限制剪贴板权限，请手动长按邀请链接输入框复制；也可以用手机相机/微信扫一扫页面上的房间二维码入局。
- **对方断线后我这边不能落子**：属于正常的"重连窗口"暂停，60 秒内对方重连会自动恢复；超时未归会提示对方离开并结束本局。
- **二维码扫不出来**：先用 `node work/verify-qr.js` 自检二维码生成是否正常（本项目已实测终端二维码可被解码器正确识别）。
  页面上的房间二维码由内嵌编码器生成，手机相机或微信扫一扫一般可直接识别；
  个别手机扫码弱时，改用自动复制的邀请链接或手动输入 5 位房间号加入。
- **点【📞 语音】没反应 / 提示需要 HTTPS**：浏览器只允许安全上下文调用麦克风/摄像头。
  **异地联机走 cpolar 时是 https，可以直接用**；若你改用局域网 IP 的 http 页面则不满足条件，
  此时可只用聊天与棋盘对局。通话按钮还需要对方在房间内且在线。
- **通话连不上（一直"正在呼叫"）**：WebRTC 依赖 NAT 打洞（公共 STUN），极端对称 NAT 下可能失败；可用 TURN 服务器中继（把 TURN 地址加入代码 `ICE_SERVERS` 配置）。
