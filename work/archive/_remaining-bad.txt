--- L2642 ---
 * 九、教育模式（人机对局专用�? * 说明（深度教学版）：玩家每落一子，AI 按优先级给出点评�? * 获胜 > 错过必杀 > 防守警告 > 双杀赞美 > 更优推荐（玩家视角，带原因）
--- L2643 ---
 * > 弱棋批评 > 棋型描述（附术语解释）；同时可显示推荐落点�? * 棋盘上还会高亮玩家最后一步的棋型线，并闪�?AI 下一步可成五的危险点�? * ============================================================ */
--- L2646 ---
 * 分析玩家刚下的一步，返回一句中文点评（深度教学版）�? * 优先级：直接获胜 > 错过必杀 > AI 下一步可成五（防守警告）> 双杀赞美
--- L2647 ---
 *        > 更优推荐（带原因�? 弱棋批评 > 棋型描述（附术语解释）�? * @param {number} r 行号
--- L2652 ---
  // �?玩家直接获胜
--- L2656 ---
  // �?玩家错过必杀：明明可以一步成五，却下在了别处
--- L2659 ---
    return '这步不是最优：你在 ' + formatPos(missed[0], missed[1]) + ' 可以直接连成五子获胜，错过了一次必杀机会�?;
--- L2661 ---
  // �?防守警告：AI 下一步可直接成五，且玩家这步没有挡住
--- L2664 ---
    return '注意防守�? + colorName(aiColor) + '下一步可�?' + formatPos(aiWin[0], aiWin[1]) + ' 连成五子，建议先挡住�?;
--- L2666 ---
  // �?双杀赞美：玩家这步形成了双三/三四/双四等不可防的组�?  const combo = playerDoubleThreatComment(r, c);
--- L2668 ---
  // �?更优推荐：用“玩家视角”计算更佳落点（颜色参数化，最多用中等强度保证响应速度�?  const best = getBestMove(eduRecommendLevel(), playerColor);
--- L2674 ---
        return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：可以直接连成五子获胜！';
--- L2678 ---
      if (bthr >= 2) return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：能形成双杀，对方很难同时防守�?;
--- L2679 ---
      if (blv === 3) return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：能形成活四，几乎锁定胜局�?;
--- L2680 ---
      if (blv === 2) return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：能形成冲四，逼对方应对�?;
--- L2681 ---
      if (blv === 1) return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：能形成活三，展开攻势�?;
--- L2682 ---
      return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：能形成更强的攻势�?;
--- L2685 ---
  // �?弱棋批评：既无进攻、也无防守价值的落子
--- L2688 ---
  // �?棋型描述（附术语解释�?  return '�? + patternText(analyzePoint(r, c, playerColor));
--- L2692 ---
 * 玩家落子后是否形成了双杀（双�?三四/双四）：统计四方向中的威胁方向数�? * 该函数在玩家落子之后调用�?r, c) 的棋已经在棋盘上�? * @returns {string|null} 双杀赞美文案；不是双杀返回 null
--- L2707 ---
  if (fours >= 2) return '妙手！你形成了双四，' + colorName(aiColor) + '无法同时防守，胜局已定�?;
--- L2708 ---
  if (fours === 1 && threes >= 1) return '漂亮！你形成了冲�?活三的组合杀�? + colorName(aiColor) + '很难同时应对�?;
--- L2709 ---
  if (threes >= 2) return '漂亮！你形成了双三，' + colorName(aiColor) + '只能堵住一边，攻势很大�?;
--- L2714 ---
 * 判断玩家刚下的一步是否为“弱棋”，并给出原因（只返回一条最要紧的）�? * 三种弱棋：漏防对方冲�?活四威胁、毫无威胁的连接、远离战场的游离棋�? * @returns {string|null}
--- L2718 ---
  const ownLv = threatLevel(r, c, playerColor);     // 落点形成的最强威�?  const blocksAi = threatLevel(r, c, aiColor) >= 1; // 恰好占了 AI 的威胁点（有防守价值）
--- L2719 ---
  if (own >= 1000 || ownLv >= 1 || blocksAi) return null; // 有活二以上攻势或有防守价�?�?不算弱棋
--- L2721 ---
  // �?AI 存在未处理的冲四/活四威胁点（只检查棋子周围，控制开销�?  let aiThreat = null;
--- L2731 ---
    return '这步没有处理' + colorName(aiColor) + '的威胁：' + colorName(aiColor) + '可在 ' + formatPos(aiThreat[0], aiThreat[1]) + ' 形成冲四/活四，建议先防守或做出更强进攻�?;
--- L2733 ---
  // �?附近�? 格内）没有任何棋�?�?游离�?  let near = false;
--- L2742 ---
    return '这步比较游离，远离了双方棋子，建议贴近战场展开攻防�?;
--- L2744 ---
  // �?一般性弱�?  return '这步没有形成有效进攻，也没有防守价值，建议找能做活�?活三的要点�?;
--- L2748 ---
 * 教学推荐使用�?AI 强度：不超过“中等”�? * 困难档全量搜索（最�?2.5s）用于实战决策；点评/推荐没必要等那么久，
--- L2749 ---
 * 用中等档即可给出高质量建议，保持交互流畅�? */
--- L2755 ---
 * 分析某点落子后形成的最大棋型（取四方向中连子数最多、威胁最大的一个）�? * @returns {{count: number, open: number, reachable: boolean}}
--- L2762 ---
    // 连子数相同时再比较威胁分；同时记录方向，供画布高亮棋型线使用�?    if (info.count > best.count ||
--- L2775 ---
    if (p.open === 2) return '形成了活四（四子连珠且两端开放），几乎锁定胜局�?;
--- L2776 ---
    if (p.open === 1) return '形成了冲四（四子连珠且单端开放），对方必须应对�?;
--- L2777 ---
    return '形成了死四：两端都被封死，无法连成五子�?;
--- L2781 ---
    if (p.open === 1 && p.reachable) return '形成了眠三，有一定威胁�?;
--- L2782 ---
    return '形成了死三：两端空间不足，难以连成五子�?;
--- L2785 ---
    return p.open === 2 ? '形成了活二，可以继续扩展�? : '形成了有限的连接�?;
--- L2787 ---
  return '完成了落子，继续观察局势�?;
--- L2803 ---
/** 更新点评面板中“对玩家的点评”一�?*/
--- L2808 ---
/** 更新点评面板中“AI 思路”一�?*/
--- L2815 ---
  playerCommentEl.textContent = '教学点评开启后，AI 会点评你的每一步�?;
--- L2820 ---
 * 九·七、胜率预测（人机对战�? * 说明：用当前局面的静态评估分数估�?AI/玩家胜率，并以色条可视化�? * 算法：先把棋盘评估分（evaluateBoard，AI 视角正分=AI 优）�?tanh 映射�? * 0~1，再缩放成百分比。三个防“假胜率”的措施�? *  1) 98% 封顶只给“轮到的一方”的一步成五（对方马上会挡的冲四不算锁定胜局）；
--- L2821 ---
 *  2) 双威胁组合分（双三候选）在胜率里�?2.5 折，强但不必胜的棋型只到 60~80%�? *  3) 映射标尺加大，普通优势不再被 tanh 放大�?90%+�? * ============================================================ */
--- L2824 ---
 * 估算当前局面的胜率（纯棋盘函数，便于测试）�? * @param {number|null} nextPlayer 即将行动的一方（BLACK/WHITE）；null=不区分回�? *   （测试用）。只有“轮到的一方”能一步成五时才锁�?98%——因为对方下一�? *   可以先挡，冲四这类棋型并不等于已经赢了�? * @returns {{player: number, ai: number}} 玩家/AI 胜率百分比（0~100�? */
--- L2831 ---
  // 静态评�?�?tanh 映射；双威胁组合分打折，避免“双三”等棋型误报�?98%
--- L2838 ---
/** 刷新胜率面板：仅人机模式且开关开启时显示；终局直接显示 100/0 �?0/100 */
--- L2847 ---
    player = winLine ? (currentPlayer === playerColor ? 100 : 0) : 50;  // PvE 终局未换�?  } else {
--- L2848 ---
    // currentPlayer �?updateStatus（换手后）调用时 = 即将行动方；
--- L2849 ---
    // 传给 estimateWinRate 让“一步成五”的 98% 锁定只对真实行动方生�?    player = estimateWinRate(currentPlayer).player;
--- L2851 ---
  const playerR = Math.round(player * 10) / 10;          // 统一四舍五入�?0.1�?  const aiR = Math.round((100 - playerR) * 10) / 10;     // 避免出现 31.700000000000003% 的浮点残�?  winRateText.textContent = '�?' + playerR + '% / AI ' + aiR + '%' + (gameOver && !winLine ? '（平局�? : '');
--- L2857 ---
 * 说明：所有交互入口集中在这里，逻辑函数只负责“做什么”�? * ============================================================ */
--- L2859 ---
// 棋盘点击统一入口：转发给当前激活的游戏适配器处�?canvas.addEventListener('click', (e) => {
--- L2865 ---
 * 五子棋的棋盘点击：把屏幕坐标换算成格子坐标后落子�? * 作为 gomokuAdapter.onCanvasClick 的实现，只处理五子棋自己的规则�? */
--- L2868 ---
  if (gameMode === MODE_PVE && currentPlayer === aiColor) return;  // AI 回合不响�?  if (gameMode === MODE_ONLINE && (!onlineReady || myColor !== currentPlayer)) return;  // 在线：未就绪或没轮到自己
--- L2870 ---
  // 屏幕坐标 �?格子坐标（四舍五入，点击格子边缘也能命中交叉点）
--- L2881 ---
// 模式切换：新模式 = 新开一局（转发给当前游戏�?btnPvp.addEventListener('click', () => {
--- L2898 ---
// 在线对战：进入在线模�?/ 创建房间 / 加入房间 / 断开连接
--- L2907 ---
    onlineStatusEl.textContent = '请输�?5 位房间号（字母或数字�?;
--- L2914 ---
// 房间号输入：自动转大写、过滤非法字符，最�?5 �?roomCodeInput.addEventListener('input', () => {
--- L2918 ---
// 游戏切换：框架级入口，切换前先让旧游戏收�?gameSelect.addEventListener('change', () => switchGame(gameSelect.value));
--- L2932 ---
// 教学点评 / 推荐点开关：属于“可选能力”，适配器实现了才响�?eduModeToggle.addEventListener('change', () => {
--- L2944 ---
// 胜率显示开�?winRateToggle.addEventListener('change', () => {
--- L2949 ---
// 猜先弹窗：切换方�?/ 掷硬�?/ 猜奇�?/ 选择执子 / 跳过
--- L2980 ---
 * 说明：把五子棋包装成 GameAdapter 注册进框架。国际象棋、围棋�? * 中国象棋的预留骨架见 games/ 目录，实现后同样 registerGame 即可�? * ============================================================ */
--- L2985 ---
  name: '五子�?,
--- L2989 ---
  aiLevels: { easy: '简�?, medium: '中等', hard: '困难' },
--- L2993 ---
  /** 进入五子棋：按当前模式重开一局，并询问“开局猜先�?*/
--- L2996 ---
  /** 切走前取消未执行�?AI 落子定时器、收起猜先弹窗，并断开在线连接 */
--- L3017 ---
  /** AI 难度变化：不影响进行中的棋局，只影响之后�?AI 落子与推荐点 */
--- L3029 ---
  /** 教学点评开�?*/
--- L3045 ---
  /** 推荐点开�?*/
--- L3057 ---
// 注册五子棋并启动；页面默认进入双人对战�?9 路棋�?registerGame(gomokuAdapter.id, gomokuAdapter);
--- L3060 ---
// 邀请链接支持：?room=房间号（�??invite=）打开页面时自动切到在线模式并加入房间�?// 配合 cpolar 公网地址，朋友在微信里点开链接即可直接入局，无需手动输入房间号�?(function autoJoinByInvite() {
--- L3072 ---
     预留接口：后续游戏骨架（开发中），�?games/ 目录�?     若这些文件缺失（例如只拷贝了�?HTML 文件），五子棋本身不受影响，
--- L3073 ---
     浏览器控制台可能提示 404，可忽略�?     ============================================================ -->