--- L695 MATCH ---
FIXED : /** 按当前棋盘格数重设画布宽高（19 路最大，�?760px�?*/
REF   : 
/** 按当前棋盘格数重设画布宽高（19 路最大，约 760px） */
--- L1161 NO-MATCH ---
FIXED :   btnFlipCoin.disabled = true;          // 掷硬�?猜奇偶都只有一次机�?  btnGuessOdd.disabled = true;
--- L1164 NO-MATCH ---
FIXED :   const guesserName = gameMode === MODE_PVE ? '�? : '玩家1';
--- L1167 MATCH ---
FIXED :     // AI 赢得猜先：自动选黑先行，玩家执白（无需手动选择�?    guessState.winnerBlack = true;
REF   :     // AI 赢得猜先：自动选黑先行，玩家执白（无需手动选择）
    guessState.winnerBlack = true;
--- L1168 NO-MATCH ---
FIXED :     guessPickTextEl.textContent = 'AI 赢得了猜先，选择执黑先行。你将执白�?;
--- L1175 NO-MATCH ---
FIXED :     guessPickTextEl.textContent = winName + ' 赢得了猜先！请选择执子�?;
--- L1185 NO-MATCH ---
FIXED :  * 应用猜先结果�? * @param {boolean|null} pickBlack true=赢家执黑 false=赢家执白 null=使用 AI 自动选择的结�? * 人机模式�?playerColor/aiColor；双人模式写 p1Color（玩�? 取相反颜色）�? */
--- L1200 MATCH ---
FIXED :   // 玩家选执�?�?AI 执黑先行，先调度 AI 首步
REF   :   // 玩家执白时 AI 执黑先行：开局直接调度 AI 首步
--- L1206 MATCH ---
FIXED :   // 教育模式下按新的玩家颜色刷新推荐点与危险点高�?  if (eduMode && gameMode === MODE_PVE && currentPlayer === playerColor && !gameOver) {
REF   :   // 教育模式下按新的玩家颜色刷新推荐点与危险点高亮
  if (eduMode && gameMode === MODE_PVE && currentPlayer === playerColor && !gameOver) {
--- L1214 NO-MATCH ---
FIXED :  * 六、胜负判�? * 说明：只检查“刚落的子”是否成五——能成五的连线必然穿过它�? * ============================================================ */
--- L1216 NO-MATCH ---
FIXED :  * 检查以 (r, c) 为最后一子是否形成五连�? * @returns {Array|null} 整条连线的坐标数组（供高亮绘制），没有则返回 null�? */
--- L1221 MATCH ---
FIXED :     // 以落子点为中心，沿当前方向向两端延伸，收集同色棋�?    const line = [[r, c]];
REF   :     // 以落子点为中心，沿当前方向向两端延伸，收集同色棋子
    const line = [[r, c]];
--- L1238 NO-MATCH ---
FIXED :  * 七、绘制（数据 �?画面�? * 说明：每次全量重绘，只读 board 与少量状态，保证画面与数据永远一致�? * ============================================================ */
--- L1241 MATCH ---
FIXED :   drawStones();     // 所有棋�?  drawLastMove();   // 最后一步标记（小红点）
REF   :   drawStones();     // 所有棋子
  drawLastMove();   // 最后一步标记（小红点）
--- L1243 NO-MATCH ---
FIXED :   drawTeaching();   // 教学模式：玩家棋型高�?+ AI 危险点闪�?  drawHint();       // 教育模式推荐点（半透明圆圈�?  updateTeachBlink(); // 按需启动/停止危险点闪烁动�?}
--- L1252 MATCH ---
FIXED :   // 网格：横、竖�?boardSize 条线
REF   : 
  // 网格：横、竖各 boardSize 条线
--- L1266 MATCH ---
FIXED :   // 星位：天�?+ 四角星。公式保�?13/15/19 路都能落在标准位�?  const center = (boardSize - 1) / 2;
REF   :   // 星位：天元 + 四角星。公式保证 13/15/19 路都能落在标准位置
  const center = (boardSize - 1) / 2;
--- L1278 MATCH ---
FIXED : /** 绘制全部棋子：黑子深色实心，白子浅色带描�?*/
REF   : 
/** 注册一个游戏适配器，并刷新“游戏”下拉框 */
--- L1305 MATCH ---
FIXED : /** 用红线连接获胜五子，结果一目了�?*/
REF   : 
/** 注册一个游戏适配器，并刷新“游戏”下拉框 */
--- L1333 MATCH ---
FIXED : /** 教学模式绘制层：高亮玩家最后一步的棋型线，并闪�?AI 下一步可成五的危险点 */
REF   : 
/** 教育模式：在 AI 推荐的落点画半透明绿色标记（仅轮到玩家时显示） */
--- L1336 MATCH ---
FIXED :   // �?玩家最后一步形成的棋型方向：黄色半透明粗线（画在棋子之上，
REF   :   // 胜率显示：仅对支持教学点评（即完整实现）的人机游戏开放
--- L1337 MATCH ---
FIXED :   //    直观展示“这步形成了活三/冲四”等棋型走向�?  if (lastMove && board[lastMove[0]][lastMove[1]] === playerColor) {
REF   :   //    直观展示“这步形成了活三/冲四”等棋型走向）
  if (lastMove && board[lastMove[0]][lastMove[1]] === playerColor) {
--- L1358 MATCH ---
FIXED :   // �?AI 下一步可成五的危险点：红色圆圈，�?300ms 节奏闪烁，提醒玩家先防守
REF   :   // ② AI 下一步可成五的危险点：红色圆圈，随 300ms 节奏闪烁，提醒玩家先防守
--- L1395 NO-MATCH ---
FIXED :  * 说明�? * - 简单：纯单步启发式打分�? * - 中等：在打分基础上增加“一步必杀/挡杀”检测、VCF 连续冲四杀棋�? *         双杀（双�?双四）威胁识别、双�?双四组合加权、威胁分层决�? *         （含“防守反击”选点）、开局库（�?5 手查表）与规则式开局策略兜底�? * - 困难：再叠加 Alpha-Beta 剪枝搜索，搜索深度随残局进度自动加深�?�?�? 层）�? *         单步思考预算最长约 4s，中后盘与残局能看穿更长的杀棋链条�? * 统一入口 getBestMove(level, forColor)：AI 落子�?aiColor 视角�? * 教学推荐�?点评�?playerColor（玩家）视角，保证建议真正利于玩家�? * 猜先功能可让玩家执白、AI 执黑，因此所�?AI 决策一律以 aiColor 为准�? * ============================================================ */
--- L1398 MATCH ---
FIXED :  * VCF（Victory by Continuous Fours，连续冲四）杀棋检测�? * 思路：连续下“冲四”，对方每一步都只能被迫挡这个冲四，
REF   :  * VCF（Victory by Continuous Fours，连续冲四）杀棋检测。
 * 思路：连续下“冲四”，对方每一步都只能被迫挡这个冲四，
--- L1399 NO-MATCH ---
FIXED :  * 一路把对方带到“活�?五连”为止——这就是一条强制胜序列�? * 命中时返回当前这一步（首个强制落点），并把完整路径存入 lastVcfPath 供教学展示�? * @param {number} color 进攻方颜�? * @param {number} maxPlies 最大搜索层数（每层 = 己方一�?+ 对方一手）
--- L1405 NO-MATCH ---
FIXED :   // 对方已可一步成�?�?VCF 前提不成立（必须先防守）�?  // 我方已可一步成�?�?交给 findImmediateWin 处理即可�?  if (findImmediateWin(opp) || findImmediateWin(color)) return null;
--- L1406 NO-MATCH ---
FIXED :   // 记录“最短连杀路径”：同一局面往往存在多条 VCF 路线，优先走手数最短的�?  // 因为对方被迫应的手数越少，越没有机会中途反杀，杀棋越稳�?  const vcf = { nodes: 0, t0: performance.now(), budget: VCF_TIME_BUDGET_MS, cur: [], bestLen: Infinity, bestPath: null };
--- L1415 MATCH ---
FIXED :  * VCF 递归主体�? * @param {number|null} lastR 上一手落点行（用于快速判五，null 表示根节点）
REF   :  * VCF 递归主体。
 * @param {number|null} lastR 上一手落点行（用于快速判五，null 表示根节点）
--- L1417 NO-MATCH ---
FIXED :  * @returns {number} 从该节点取胜所需最少“己方手数”（>0 表示必胜�?1 表示无解/超限�? */
--- L1419 NO-MATCH ---
FIXED :   if (++vcf.nodes > VCF_NODE_LIMIT) return -1;               // 节点上限防卡�?  if (performance.now() - vcf.t0 > vcf.budget) return -1;    // 时间上限防卡�?  if (lastR !== null && canWinNow(lastR, lastC, color)) {
--- L1420 MATCH ---
FIXED :     if (vcf.cur.length < vcf.bestLen) {                      // vcf.cur 即“根到当前手”完整路�?      vcf.bestLen = vcf.cur.length;
REF   :     if (vcf.cur.length < vcf.bestLen) {                      // vcf.cur 即“根到当前手”完整路径
      vcf.bestLen = vcf.cur.length;
--- L1437 MATCH ---
FIXED :       len = 1;                              // 直接成五或形成活四：对方无法一手化�?      if (vcf.cur.length < vcf.bestLen) {
REF   :       len = 1;                              // 直接成五或形成活四：对方无法一手化解
      if (vcf.cur.length < vcf.bestLen) {
--- L1444 MATCH ---
FIXED :         len = 1;                            // 双四：对方只能堵一处，另一处照样成�?        if (vcf.cur.length < vcf.bestLen) {
REF   :       len = 1;                              // 直接成五或形成活四：对方无法一手化解
      if (vcf.cur.length < vcf.bestLen) {
--- L1453 MATCH ---
FIXED :         if (sub > 0) len = sub + 1;         // 对方堵住后我们仍能连杀，总手�?+1
REF   :         if (sub > 0) len = sub + 1;         // 对方堵住后我们仍能连杀，总手数 +1
--- L1458 MATCH ---
FIXED :     // 只保留手数最短的分支：杀棋越短越稳（vcfForcingMoves 已按威胁等级排序�?    if (len > 0 && len < bestLen) { bestLen = len; bestMove = [r, c]; }
REF   :     // 只保留手数最短的分支：杀棋越短越稳（vcfForcingMoves 已按威胁等级排序）
    if (len > 0 && len < bestLen) { bestLen = len; bestMove = [r, c]; }
--- L1460 MATCH ---
FIXED :   // 根节点处把最短路径起点记录下来，�?findVcfWin 返回
REF   :   // 根节点处把最短路径起点记录下来，供 findVcfWin 返回
--- L1468 MATCH ---
FIXED : /** 收集“落子后能形成冲�?活四/五连”的候选点，按威胁等级与启发式分降�?*/
REF   : 
/** 主动断开连接并清理在线状态（离开在线模式 / 切换游戏时也会调用） */
--- L1492 MATCH ---
FIXED : /** 我方�?(r, c) 落子形成冲四后，对方必须堵住的开放端点（冲四只有一个端点） */
REF   : /** (r, c) 是否在棋盘范围内 */
--- L1516 MATCH ---
FIXED :  * 双杀检测：一手落子后形成“不可防”的双重威胁（双�?/ 活四+任意），
REF   :  * 双杀检测：一手落子后形成“不可防”的双重威胁（双四 / 活四+任意），
--- L1517 MATCH ---
FIXED :  * 对方一步只能处理其中一处，另一处下一手必然成五�? * @returns {Array|null} [r, c]
REF   :  * 对方一步只能处理其中一处，另一处下一手必然成五。
 * @returns {Array|null} [r, c]
--- L1527 MATCH ---
FIXED :       let forcing = 0;                        // 冲四/活四方向�?      for (const [dr, dc] of DIRECTIONS) {
REF   :       let forcing = 0;                        // 冲四/活四方向数
      for (const [dr, dc] of DIRECTIONS) {
--- L1537 NO-MATCH ---
FIXED :       if (maxLv < 3 && forcing < 2) continue; // 只有活四�?）或双冲四（2+2）才算不可防
--- L1546 NO-MATCH ---
FIXED :  * 双三/双威胁“阵法杀招”检测：一手落子后形成两个“活三以上”威�? * （双�?/ 活三+冲四 / 双四），对方一步只能堵一处，是五子棋最典型�? * 必胜阵型——中盘把握住它就能把优势直接兑现成杀棋�? * 前提：对方没有更快威胁（不能一步成五、也不能一手成活四），
--- L1547 NO-MATCH ---
FIXED :  * 否则必须先防守，双杀只是空谈�? * @param {number} color 行动方颜�? * @returns {Array|null} 杀招落�?[r, c]；不存在则返�?null
--- L1551 NO-MATCH ---
FIXED :   if (findImmediateWin(opp)) return null;   // 对方能一步成�?�?必须先挡
--- L1556 NO-MATCH ---
FIXED :       // 比我方双三（约三子内成五）更快，必须放弃进攻先防�?      if (threatLevel(r, c, opp) >= 3) return null;
--- L1570 NO-MATCH ---
FIXED :  * 找出“对方下一步落子即可一手形�?�? 个活三以上威胁（双三/三四/双四）”的空位�? * 这类点一旦让给对方，对方就拥有不可防的强制胜，因此我方必须抢先占住�? * 选点优先“挡住后自己还能顺势反击”的位置（用己方视角�?scoreFor 决胜）�? * @param {number} color 对方颜色
--- L1571 MATCH ---
FIXED :  * @returns {Array|null} 需要堵住的�?[r, c]；不存在则返�?null
REF   :  * @returns {Array|null} 需要堵住的点 [r, c]；不存在则返回 null
--- L1580 MATCH ---
FIXED :       // countThreats 会“假装”在该点落子统计威胁数（lineInfo 不写棋盘�?      if (countThreats(r, c, color) >= 2) {
REF   :       // countThreats 会“假装”在该点落子统计威胁数（lineInfo 不写棋盘）
      if (countThreats(r, c, color) >= 2) {
--- L1590 NO-MATCH ---
FIXED :  * 开局策略（AI 前若干手）：把落点限制在中心 9×9 区域�? * 优先选“与已有己方子构成两个方向发展”的点；对方占天元时呼应天元旁星位�? * 从得分最高的前几名里带权随机选一手，�?AI 面对同一种开局时出棋不重样�? * @param {string} [level] 当前难度，决定随机候选数（简单变化大、困难收敛）
--- L1594 MATCH ---
FIXED :   // 用棋盘上的棋子数判断是否仍在开局阶段（不依赖外部状态，便于测试�?  let placed = 0;
REF   :   // 用棋盘上的棋子数判断是否仍在开局阶段（不依赖外部状态，便于测试）
  let placed = 0;
--- L1609 MATCH ---
FIXED :       // 双方向发展：落子后能在两个不同方向延伸已有己方子 �?开局更灵�?      let dirs = 0;
REF   :       // 双方向发展：落子后能在两个不同方向延伸已有己方子 → 开局更灵活
      let dirs = 0;
--- L1624 MATCH ---
FIXED :   candidates.sort((a, b) => b.s - a.s);             // 分数降序，供 pickVaried 取头�?  return pickVaried(candidates, pickTopN(level, 'opening'));
REF   :   candidates.sort((a, b) => b.s - a.s);             // 分数降序，供 pickVaried 取头部
  return pickVaried(candidates, pickTopN(level, 'opening'));
--- L1627 NO-MATCH ---
FIXED :  * 开局库查询：把当前局面做 8 对称规范化后查表，命中则返回库内应答�? * 开局库由 work/gen-opening-book.js 用引擎自身深�?2 搜索生成，覆�? * 盘面 0~5 子（�?5 手）。命中时 AI 开局有章法、响应快，且同一开局�? * 按权重随机换着下（配合 moveVariety�?=恒取最强，>0 按权�?5~1 随机�? * hard 只在�?2 名里浮动、medium �?3 名，兼顾强度与“变幻莫测”）�? * @param {string} level 难度档位，决定随机候选范�? * @param {number} side 行动方颜色（BLACK/WHITE），决定查表 key 的前缀
--- L1641 NO-MATCH ---
FIXED :   // 8 对称变换表（与生成器一致）：相对天元坐�?(x,y)=(r-center, c-center)�?  // INV 为逆变换，把库内“规范朝向”的相对坐标还原到当前棋盘的实际朝向
--- L1649 NO-MATCH ---
FIXED :   // 规范化：�?8 个朝向里“字典序最小的棋子坐标串”作�?key�?  // 同一开局无论旋转/镜像都命中同一�?key（生成器与这里完全一致）
--- L1669 NO-MATCH ---
FIXED :   // 按难度限制随机候选数并带权选取（权�?5~1 = 生成器的搜索分排序）
--- L1682 NO-MATCH ---
FIXED :   // 逆对称还原绝对坐标；13/15 小棋盘越界或目标已被占时返回 null，交由上层兜�?  const [x, y] = SYM[INV[bestSym]](entries[idx][0], entries[idx][1]);
--- L1691 NO-MATCH ---
FIXED :  * 获取当前难度下的最佳落点�? * @param {string} level 难度档位（LEVEL_EASY / LEVEL_MEDIUM / LEVEL_HARD�? * @param {number} [forColor] 计算视角的颜色；缺省�?aiColor（AI 实战决策）�? *        教学推荐点时�?playerColor，用“玩家视角”计算，保证建议真正利于玩家�? * @returns {Array|null} [r, c]
--- L1698 NO-MATCH ---
FIXED :     // 简单档保持“入门”定位：不做复杂威胁分层，但最基本的“能赢就赢�?    // 对方要赢了必须挡”不能漏，否�?AI 会犯低级失误、体验很差�?    const easyWin = findImmediateWin(me);
--- L1705 NO-MATCH ---
FIXED :   // 中等/困难档：完整威胁分层（一步成�?�?VCF �?双杀 �?活四/活三攻防 �?开局�?�?启发式开局 �?搜索�?  {
--- L1706 MATCH ---
FIXED :     const aiWin = findImmediateWin(me);            // 自己能一步成�?�?直接�?    if (aiWin) return aiWin;
REF   :     const aiWin = findImmediateWin(me);            // 自己能一步成五 → 直接赢
    if (aiWin) return aiWin;
--- L1707 MATCH ---
FIXED :     const playerWin = findImmediateWin(opp);       // 对方能一步成�?�?必须�?    if (playerWin) return playerWin;
REF   :     const playerWin = findImmediateWin(opp);       // 对方能一步成五 → 必须挡
    if (playerWin) return playerWin;
--- L1715 MATCH ---
FIXED :     // 双杀：一手形成双�?活四等不可防威胁
REF   :   // 胜率显示：仅对支持教学点评（即完整实现）的人机游戏开放
--- L1721 MATCH ---
FIXED :     // 双三/三四/双四“阵法杀招”：一手形成双威胁且对方无更快威胁 �?主动建立必胜阵型
REF   :                                    // 用于搜索评估：双方各有一个活三时，先手方把活三
                                   // 变活四即可锁定胜局，静态评估若只看棋型会误判成均势。
--- L1724 NO-MATCH ---
FIXED :     // 对方双杀意图防守：对方下一步一手可成双�?三四/双四 �?抢先堵住关键�?    const oppDt = findOpponentDoubleThreat(opp);
--- L1726 NO-MATCH ---
FIXED :     // 常规攻防：己方活三（进攻优先）与防守反击选点；不强制堵对方眠三开放端�?    // 把“堵还是进攻”交给搜�?评分按全局分数权衡，避免过度防�?    const threat = resolveThreats(me, opp, false, false);
--- L1728 NO-MATCH ---
FIXED :     // 开局库（盘面 0~8 子）：没有即时战术时才按库内定式应手（引擎自身搜索生成，带权随机增加变化）�?    // 放在战术层之后：更深的开局局面也可能出现活三/冲四等威胁，先保证战术正确，安静局面再走定�?    const book = bookMove(level, me);
--- L1730 NO-MATCH ---
FIXED :     // 启发式开局策略�?~8 子兜底，开局库未覆盖时按套路布局，带随机出棋不重样）
--- L1737 NO-MATCH ---
FIXED :   // 中等档：在威胁分层（活三/冲四/双杀等）之上叠加 3 层浅搜索�?  // 让中盘的“布阵”不再只看单步打分，而是向前多看两三手的发展
--- L1743 NO-MATCH ---
FIXED :  * 单步启发式：遍历所有空位，综合“进攻分（自己成五）”�? * “防守分（阻挡玩家）”与“中心偏好”选出得分最高的位置�? */
--- L1745 NO-MATCH ---
FIXED :  * 单步启发式：遍历所有空位，综合“进攻分（自己成五）”�? * “防守分（阻挡对方）”与“中心偏好”选出得分最高的位置�? * 同分/接近分时在前 topN 名里带权随机，避免同一局面永远下同一手�? * @param {string} [level] 当前难度，决定随机候选数
--- L1749 MATCH ---
FIXED :   const center = (boardSize - 1) / 2;           // 中心坐标随格数变�?
REF   :   const center = (boardSize - 1) / 2;           // 中心坐标随格数变化
--- L1754 NO-MATCH ---
FIXED :       const attack = evaluateCell(r, c, me);    // 进攻：自己连�?      const defend = evaluateCell(r, c, opp);   // 防守：阻挡对�?      // 阵法加成：一手能形成双威胁（双三/三四/双四）是五子棋最强的进攻结构�?      // 安静局面下优先布这种“杀招阵型”，而不是零散地凑单线�?      const dtBonus = countThreats(r, c, me) >= 2 ? DOUBLE_THREAT_BONUS : 0;
--- L1760 MATCH ---
FIXED :   candidates.sort((a, b) => b.s - a.s);         // 分数降序，供 pickVaried 取头�?  return pickVaried(candidates, pickTopN(level));
REF   :   candidates.sort((a, b) => b.s - a.s);         // 分数降序，供 pickVaried 取头部
  return pickVaried(candidates, pickTopN(level));
--- L1763 MATCH ---
FIXED : /** 假设�?(r, c) 放一�?color 棋，四个方向连子得分之和（含组合加权�?*/
REF   : 
/** 假设在 (r, c) 放一颗 color 棋，四个方向连子得分之和（含组合加权） */
--- L1769 NO-MATCH ---
FIXED :   // 近似“双�?双四”：同一落点能同时形成两个活三级别的威胁时加权翻倍�?  // 这是五子棋里极难防守的棋形，必须给予额外权重�?  if (total >= LIVE_THREE_SCORE * 2) total *= 2;
--- L1774 MATCH ---
FIXED :  * 统计�?(dr, dc) 方向�?(r, c) 为中心的连子信息�? * 关键改进：除了连子数与开放端，还计算“这条线是否还有足够空间真正连成五子”，
REF   :  * 统计沿 (dr, dc) 方向以 (r, c) 为中心的连子信息。
 * 关键改进：除了连子数与开放端，还计算“这条线是否还有足够空间真正连成五子”，
--- L1775 MATCH ---
FIXED :  * 避免 AI 去凑“两端被封死、只有四个子的空间”的死棋型�? * @returns {{count: number, open: number, reachable: boolean}}
REF   :  * 避免 AI 去凑“两端被封死、只有四个子的空间”的死棋型。
 * @returns {{count: number, open: number, reachable: boolean}}
--- L1778 MATCH ---
FIXED :  *   reachable  是否有足够空间连成五：count + 两端连续空位�?>= 5
REF   :  *   reachable  是否有足够空间连成五：count + 两端连续空位数 >= 5
--- L1787 MATCH ---
FIXED :     for (; i < 5; i++) {            // 数同色连�?      const nr = r + dr * i * dir;
REF   :     for (; i < 5; i++) {            // 数同色连子
      const nr = r + dr * i * dir;
--- L1789 MATCH ---
FIXED :       if (!inBoard(nr, nc)) break;  // 越界：该端封�?      if (board[nr][nc] === color) {
REF   :       if (!inBoard(nr, nc)) break;  // 越界：该端封闭
      if (board[nr][nc] === color) {
--- L1795 MATCH ---
FIXED :     // 连子结束后，数这一端还能连续放下几个空位（最�?4 个）
REF   :     // 连子结束后，数这一端还能连续放下几个空位（最多 4 个）
--- L1808 NO-MATCH ---
FIXED :   // 可达性：连子�?+ 两端空位数必�?�?5，否则永远凑不成五连�?  // 例：B X X X _ B 的“眠三”只�?1 个空位，3+1=4 < 5，实际是死棋型�?  const reachable = count + totalSpace >= 5;
--- L1812 MATCH ---
FIXED : /** 沿某方向的连子得分（�?lineInfo 计算出的棋型查表得到�?*/
REF   : 
/** 沿某方向的连子得分（由 lineInfo 计算出的棋型查表得到） */
--- L1818 NO-MATCH ---
FIXED : /* 棋型评分表（�?连续同色子数，列=开放端�?0/1/2）：
--- L1819 NO-MATCH ---
FIXED :  * 连得越多、两端越开放，威胁越大。所有静态评估的基础分都查这一张表�? * 后续调“棋型价值”只需改这里一处。注意：reachable=false 的“死棋型�? * （死�?死三等）永远凑不成五连，lineScore 直接�?0，不查表�? * 数值沿用旧版公式：活四(10�? >> 冲四(1�? > 活三(1�? > 眠三(1�? > 活二(1�?�? */
--- L1826 NO-MATCH ---
FIXED :   [1000000, 1000000, 1000000], // 5 子及以上：已成五�?];
--- L1829 NO-MATCH ---
FIXED :  * 连子评分（查棋型表）：连得越多、两端越开放，威胁越大�? * reachable=false 的死棋型（死�?死三等）永远无法成五，直接记 0 分，
--- L1830 MATCH ---
FIXED :  * 避免 AI 为了凑四子而在注定被堵死的线上浪费一手�? */
REF   :  * 推荐点只在“AI 已经应手、轮到玩家”时计算，保证提示针对最新局面。
 */
--- L1833 NO-MATCH ---
FIXED :   if (!reachable) return 0;                                    // 空间不足，永远无法成�?  return PATTERN_TABLE[count][open] || 0;
--- L1837 NO-MATCH ---
FIXED :  * 威胁等级：在 (r, c) �?color 后，四个方向中能形成的最强威胁�? * 这是“大局观”的基础指标——AI 依据它判断一步棋究竟是普通落子�? * 活三、冲四还是活四，从而决定进攻还是防守�? * @returns {number} 0=普通落�?1=活三 2=冲四 3=活四 4=五连
--- L1845 NO-MATCH ---
FIXED :     else if (info.count === 4 && info.open === 2) lv = 3;  // 活四：两端都开，无�?    else if (info.count === 4 && info.open === 1) lv = 2;  // 冲四：单端开，逼对方应�?    else if (info.count === 3 && info.open === 2) lv = 1;  // 活三：能发展成活�?    if (lv > best) best = lv;
--- L1851 NO-MATCH ---
FIXED :  * 一步必杀检测：若在 (r, c) �?color 能立刻成五则返回该点�? * �?canWinNow 直接数连子，不修改棋盘，速度比“临时落子再判胜”快得多�? */
--- L1862 MATCH ---
FIXED : /** 快速判断：�?color 放在 (r, c) 后是否形成五连（不修改棋盘） */
REF   : 
/** 快速判断：把 color 放在 (r, c) 后是否形成五连（不修改棋盘） */
--- L1885 NO-MATCH ---
FIXED :  * 带权随机选点：在得分最高的 topN 个候选中按“排名权重”随机取一�? * （第 1 名权�?1、第 2 �?1/2、第 3 �?1/3……）�? * 效果：面对对方同一种开局，AI 不再永远下同一手，而是从几个同等级
--- L1886 NO-MATCH ---
FIXED :  * 的好点里随机应变；头部仍大概率命中，强度损失很小�? * moveVariety=0 时退化为取第一名，保证确定性（自动化测试场景）�? * @param {Array<{r:number,c:number,s:number}>} list 已按分数降序的候选列�? * @param {number} topN 参与随机的前几名
--- L1909 NO-MATCH ---
FIXED :  * 各难度出棋的随机候选数：难度越高越收敛�? * easy 变化最大（新手也能看到多种走法），hard 只在�?2 名里小幅浮动�? */
--- L1918 NO-MATCH ---
FIXED :  * 威胁分层决策（中�?困难共用，在“一步成五”与 VCF/双杀检测之后调用）�? * 解决三个痛点�? * �?以前 AI 只挡“必成五”，会漏掉对方的活三/眠三（三个子）；
--- L1919 MATCH ---
FIXED :  * �?残局空位变少时，AI 仍能按威胁优先级行动，而不是东一子西一子乱下；
REF   :  * 说明：所有可调参数集中在此，改棋盘格数、AI 强度等无需翻找逻辑代码。
--- L1920 NO-MATCH ---
FIXED :  * �?以前挡棋只求“挡住”，不挑位置——现在优先选“挡住又能反击”的落点�? *    把被动防守变成主动进攻，从而更快赢下对局�? * 优先级（从高到低）：
--- L1921 NO-MATCH ---
FIXED :  *   自己活四 > 挡对方活�?> 挡对方冲四端点（眠三开放端，可选）> 自己活三 > 交给搜索/评分�? * skipOwnAttack=true 时跳过“自己活三”这一档，只返回紧急攻防—�? * 供“先判断对方双杀意图、再决定是否进攻”的调用顺序使用�? * blockRushFours=false 时不强制堵“对方眠三的开放端”（对方三连一端已死）�? * 这种棋型对方即使冲四也只是一步一步被挡，并不构成杀棋；把这一步让�? * 搜索/评分去权衡，AI 就能优先做能赢的棋，而不是机械地处处设防�? * 只有对方存在 VCF 连续冲四杀棋时（getBestMove �?VCF 分支）才强制堵冲四端点�? * 颜色可参数化：AI 决策默认 (aiColor, playerColor)；教学推荐玩家时�?(playerColor, aiColor)�? * @param {boolean} [skipOwnAttack] true=不返回自己活三（留给更高级的进攻判断�? * @param {boolean} [blockRushFours] true=对方眠三开放端（冲四形成点）也强制�? * @returns {Array|null} 需要立即执行的落点 [r, c]，没有紧急威胁则返回 null
--- L1928 NO-MATCH ---
FIXED :   // 防守反击加分：挡棋点若同时能让我方形成活�?1)/冲四(2)/活四(3)�?  // 额外加权，让 AI 尽量选“既挡得住、又能顺势反击”的位置�?  // 为什么单独再加一次？scoreFor 的进攻分把低价值连线（活一/眠二）也算进来，
--- L1929 MATCH ---
FIXED :   // 而这里只奖励“真正的强威胁”（活三以上），保证防守选点瞄准反击，而不是零碎连线�?  const counterScore = (r, c) => {
REF   :   // 而这里只奖励“真正的强威胁”（活三以上），保证防守选点瞄准反击，而不是零碎连线。
  const counterScore = (r, c) => {
--- L1934 NO-MATCH ---
FIXED :   // 同等级威胁用启发式分 + 防守反击�?+ 中心偏好决胜�?  // 进攻点只看启发式分与中心偏好；防守点额外叠加反击分，选“能赢的位置”去挡�?  const tieScore = (r, c, defensive) =>
--- L1956 MATCH ---
FIXED :   if (playerBest && playerBest.lv >= 3) return [playerBest.r, playerBest.c]; // 对方成活四：必须�?  if (blockRushFours && playerBest && playerBest.lv === 2) return [playerBest.r, playerBest.c]; // �?VCF 防守等场景强制堵冲四端点
REF   :   if (playerBest && playerBest.lv >= 3) return [playerBest.r, playerBest.c]; // 对方成活四：必须挡
  if (blockRushFours && playerBest && playerBest.lv === 2) return [playerBest.r, playerBest.c]; // 仅 VCF 防守等场景强制堵冲四端点
--- L1963 NO-MATCH ---
FIXED :  * 生成候选落点：只收集“已有棋子周�?HINT_RADIUS 格内”的空位�? * 再按启发式得分降序取�?limit 个�? * 理由：远离棋子的落点在开局阶段几乎无意义，裁剪可大幅缩小搜索树�? */
--- L1992 MATCH ---
FIXED :   scored.sort((a, b) => b.s - a.s);   // 降序：高分候选在前，剪枝更有�?  return scored.slice(0, limit).map(({ r, c }) => [r, c]);
REF   :   scored.sort((a, b) => b.s - a.s);   // 降序：高分候选在前，剪枝更有效
  return scored.slice(0, limit).map(({ r, c }) => [r, c]);
--- L1996 MATCH ---
FIXED :  * 全局面静态评估：AI 方总进攻分 �?玩家方总进攻分 × 1.1�? * 关键改进：每条连线只从“起点”计一次分，避免一条活三被三个子重复计 3 倍，
REF   :  * 全局面静态评估：AI 方总进攻分 − 玩家方总进攻分 × 1.1。
 * 关键改进：每条连线只从“起点”计一次分，避免一条活三被三个子重复计 3 倍，
--- L1997 MATCH ---
FIXED :  * 棋盘越满评估越失真。防守权重略高（1.1），�?AI 攻防取舍时稍微偏保守�? */
REF   :  * 棋盘越满评估越失真。防守权重略高（1.1），让 AI 攻防取舍时稍微偏保守。
 */
--- L1999 NO-MATCH ---
FIXED :  * 威胁空间统计（对方威胁空间的评估基础）：围绕 color 棋子，统计“落子后�? * 形成活三以上威胁”的空位，并按威胁等级加权（活三 600 / 冲四 2000 /
--- L2001 NO-MATCH ---
FIXED :  * 能延伸成活四的选点多的一方更主动；对方威胁空间越大，越不能安心进攻�? * �?comboBonus 的分工：comboBonus 只奖励“一手双威胁”的杀招点，这里把
--- L2002 NO-MATCH ---
FIXED :  * 普通单线威胁也算进来，让叶节点评估不再只盯着必杀点、有全局大局观�? * 数量上限 cap 控制评估开销（minimax 叶节点调用非常频繁）�? */
--- L2028 NO-MATCH ---
FIXED :  * 局面评估：己方全部棋型�?�?对方全部棋型分�?.1，另加连接性、中心权重�? * 双威胁组合分与“威胁空间”分（见 threatSpaceBonus）�? * @param {number} [comboWeight] 双威�?威胁空间分的折扣系数：AI 决策�?1�? *        胜率估算�?0.25，避免“双�?活三延伸”这类强而不必胜的棋型把胜率推过�? */
--- L2040 MATCH ---
FIXED :         // 只统计这条连线的起点（r-dr, c-dc 不是同色子），一行只计一�?        const pr = r - dr, pc = c - dc;
REF   :         // 只统计这条连线的起点（r-dr, c-dc 不是同色子），一行只计一次
        const pr = r - dr, pc = c - dc;
--- L2047 MATCH ---
FIXED :       // 连接性：与同色相邻的子数（鼓励进攻抱团，避免单子乱飞�?      let adj = 0;
REF   :       // 连接性：与同色相邻的子数（鼓励进攻抱团，避免单子乱飞）
      let adj = 0;
--- L2059 NO-MATCH ---
FIXED :   // comboWeight：默�?1（AI 决策用，双三=强杀招）；胜率估算时传入折扣系数�?  // 让“双三候选”这类强而不必胜的棋型不至于把胜率推�?98% 的封顶值�?  aiScore += comboBonus(me) * comboWeight;
--- L2061 NO-MATCH ---
FIXED :   // 威胁空间：双方各有多少个“一手成活三以上”的选点（进攻灵活度/对方反击空间）�?  // 双威胁杀招由 comboBonus 单独计，这里只补单线威胁，避免重复；对方威胁空间
--- L2062 NO-MATCH ---
FIXED :   // 同样×1.1，让 AI 进攻时始终把对手的反击空间考虑进去（全局大局观）�?  const aiSpace = threatSpaceBonus(me) * comboWeight;
--- L2067 NO-MATCH ---
FIXED :   // 静态棋型分完全一样，但先手方下一手就能把活三变活四锁定胜局—�?  // 不修正会让浅层搜索严重误判这类“先手决定胜负”的局面�?  if (tempoFor === me) return raw + TEMPO_BONUS;
--- L2074 NO-MATCH ---
FIXED :  * 对方一步只能堵一处，是极难防守的杀棋。只扫描“己方棋子相邻”的空位�? * 并设数量上限，控制评估开销（否�?minimax 叶节点会明显变慢）�? */
--- L2098 MATCH ---
FIXED :  * 统计�?(r, c) �?color 后，四个方向中“有效威胁”（活三及以上：
REF   :  * 威胁等级：在 (r, c) 落 color 后，四个方向中能形成的最强威胁。
--- L2099 MATCH ---
FIXED :  * 活三/冲四/活四/五连）的数量。威胁数 �?2 意味着对方一手无法同时处理�? */
REF   :  * 活三/冲四/活四/五连）的数量。威胁数 ≥ 2 意味着对方一手无法同时处理。
 */
--- L2111 NO-MATCH ---
FIXED :  * 初始化搜索加速表（置换表 + 历史启发）�? * 每次 bestBySearch 开始时调用：重建棋盘哈希、清空置换表，保�? * 哈希随机底数与棋盘状态一致（棋盘可能因前一手落子而变化）�? */
--- L2114 NO-MATCH ---
FIXED :     // 首次生成 Zobrist 随机底数：每�?(�?�?颜色) 一�?32 位随机整数�?    // �?19×19 固定尺寸�?3/15 棋盘只使用左上部分，不影响哈希正确性�?    ttZobrist = [];
--- L2125 NO-MATCH ---
FIXED :   // 跨步置换表：ttMap / historyTable / killerTable 在整局内跨落子持续复用�?  // 相同局面（含“轮到谁”与搜索视角）的搜索结果直接复用，后续每一步都�?  // 吃到前几步搜索积累的缓存，同一预算下看得更深。哈希随机底数只在首�?  // 生成、整页稳定；ttStore 在条目超上限时会整体清空，内存有界�?  if (!ttMap) ttMap = new Map();
--- L2136 MATCH ---
FIXED : /** �?(r,c) 处的 color 棋从棋盘哈希中异或进/出（落子与悔棋各调用一次） */
REF   : 
/** 把 (r,c) 处的 color 棋从棋盘哈希中异或进/出（落子与悔棋各调用一次） */
--- L2142 MATCH ---
FIXED : /** 写置换表：窗口过窄会�?LOWER/UPPER，完整搜索存 EXACT */
REF   : 
/** 写置换表：窗口过窄会存 LOWER/UPPER，完整搜索存 EXACT */
--- L2150 NO-MATCH ---
FIXED :  * Alpha-Beta 剪枝搜索（困难档核心）�? * 思路：轮流假�?AI（取最大）与玩家（取最小）落子，向前看 searchDepth 决定的层数，
--- L2151 NO-MATCH ---
FIXED :  * �?alpha/beta 剪掉不可能影响结果的子树；配合置换表（缓存重复局面）�? * 历史启发（好手优先尝试），同一预算能搜得更深。超预算时截断为静态评估�? */
--- L2153 MATCH ---
FIXED :   // 超时保护：预算耗尽后直接返回静态评估，保证 19 路棋盘下不卡�?  if (searchState && performance.now() - searchState.t0 > searchState.budget) {
REF   :   // 超时保护：预算耗尽后直接返回静态评估，保证 19 路棋盘下不卡顿
  if (searchState && performance.now() - searchState.t0 > searchState.budget) {
--- L2156 NO-MATCH ---
FIXED :   // 置换表查表：同一局面（含“轮到谁�?“搜索视角”）的搜索结果直接复用�?  // 迭代加深会反复展开同一局面，命中后能省掉整棵子树的重算�?  // 视角 XOR：评估分�?me 为正（AI 执黑与教学推荐玩家时的正负方向相反）�?  // 不加视角会把两个方向的分数混用导致误判；加上后两者各自独立缓存�?  const ttKey = boardHash ^ (isMax ? TT_SIDE_ME : TT_SIDE_OPP)
--- L2166 MATCH ---
FIXED :   // 任一方向存在一步成�?�?立即返回必胜/必败分，无需继续搜索
REF   :   // 猜先开关：双人/人机模式通用，在线模式固定黑白（房主黑、加入者白），无需猜先
--- L2171 MATCH ---
FIXED :   // ===== 威胁感知的候选生成（强制走法剪枝�?=====
REF   : 
  // ===== 威胁感知的候选生成（强制走法剪枝） =====
--- L2172 NO-MATCH ---
FIXED :   // 五子棋的胜负由“活�?冲四”这类强制威胁驱动：一旦某方存在冲四级威胁�?  // 对方只能被迫回应。因此在搜索节点先判断双方威胁，把候选收窄到�?  //   �?对方可一手成活四/成五 �?必须优先堵（除非自己能抢先活�?成五）；
--- L2173 NO-MATCH ---
FIXED :   //   �?自己能形成冲四级威胁 �?只搜这些强制点（等于�?VCF 思路融进搜索�?  //      分支数从十几个降到几个，同一预算下能顺着杀棋链多看 2~3 层）�?  //   �?否则才回落到常规候选（棋子周围 radius 2 内的启发式选点）�?  const side = isMax ? me : opp;                // 本节点行动方
--- L2181 NO-MATCH ---
FIXED :     moves = myDanger;                           // 自己能活�?成五：无解进攻优�?  } else {
--- L2183 NO-MATCH ---
FIXED :     // 单一冲四会被对方一手堵死，若强制只搜它，AI 会盲目冲四挥霍机会—�?    // 普通冲四放回常规候选（getCandidateMoves 仍按启发式把冲四点排得很前）�?    const myForcing = forcingMovesOf(side, 2).filter(([r, c]) => countThreats(r, c, side) >= 2);
--- L2186 NO-MATCH ---
FIXED :   moves = moves.slice(0, CANDIDATE_LIMIT);      // 仍然限宽，控制分支因�?  // 历史启发：把此前“引发剪�?拿到高分”的落点提前尝试，让剪枝更早发生�?  // 开局阶段历史分全�?0，排序保持原启发式顺序（Array.sort 稳定）�?  if (historyTable) {
--- L2192 NO-MATCH ---
FIXED :   // 候选的顺序、不新增落点，因此强制走法列表也不会被破坏�?  if (killerTable && killerTable[depth]) {
--- L2200 MATCH ---
FIXED :   if (moves.length === 0) { ttStore(ttKey, depth, TT_EXACT, 0); return 0; }  // 无子可下（平局�?
REF   :   if (moves.length === 0) { ttStore(ttKey, depth, TT_EXACT, 0); return 0; }  // 无子可下（平局）
--- L2201 MATCH ---
FIXED :   if (isMax) {                                       // 我方回合：取最�?    let best = -Infinity;
REF   :   if (isMax) {                                       // 我方回合：取最大
    let best = -Infinity;
--- L2221 MATCH ---
FIXED :   } else {                                           // 对方回合：取最�?    let best = Infinity;
REF   :   } else {                                           // 对方回合：取最小
    let best = Infinity;
--- L2245 NO-MATCH ---
FIXED :  * 收集 color 一步即可形成“冲四级以上威胁”（minLv=2 冲四 / minLv=3 活四 / 成五）的空位�? * 用于强制走法搜索：这类点落子后对方必须回应，是五子棋的“强制手”�? * @param {number} color 行动方颜�? * @param {number} [minLv] 最低威胁等级（默认 2�? * @returns {Array<Array<number>>} [r, c] 列表
--- L2252 MATCH ---
FIXED :       // threatLevel 对“成五”返�?4，天�?>= minLv，无需单独�?canWinNow
REF   :       // threatLevel 对“成五”返回 4，天然 >= minLv，无需单独判 canWinNow
--- L2260 MATCH ---
FIXED :  * 动态搜索深度：根据盘面棋子数自动加深层数，兼顾速度与“快点赢”�? * 开局/中盘候选点多，保持 3 层保证响应速度（约 <1s）；
REF   :  * 动态搜索深度：根据盘面棋子数自动加深层数，兼顾速度与“快点赢”。
 * 开局/中盘候选点多，保持 3 层保证响应速度（约 <1s）；
--- L2261 NO-MATCH ---
FIXED :  * 中后盘与残局时棋子密集、候选点变少，搜索树天然缩小�? * 借助置换�?历史启发压住重复展开后，可放心加深到 5~6 层，
--- L2262 NO-MATCH ---
FIXED :  * �?AI 看穿更长的杀棋链条，尽早兑现优势、避免拖到百步�? * @returns {number} 本次搜索使用的深�? */
--- L2270 NO-MATCH ---
FIXED :   if (placed >= 60) return 6;   // 残局后期：候选点很少，可放心加深�?6 �?  if (placed >= 35) return 5;   // 中后盘：候选点已收敛，加深两层尽早兑现优势
--- L2274 MATCH ---
FIXED : /** 困难档入口：在根节点对每个候选落子模拟一步，再递归搜索并选最�?*/
REF   : 
/** 注册一个游戏适配器，并刷新“游戏”下拉框 */
--- L2276 NO-MATCH ---
FIXED :  * 困难档搜索入口：迭代加深 + Alpha-Beta�? * 先搜 3 层拿到基准结果，预算内继续加深到 5~6 层（searchDepth 按残局进度定上限）�? * 时间不够就用已完成的最深一层结果；配合置换表与历史启发，同一预算能搜得更深�? * @param {string} [level] 当前难度，仅用于兜底打分时的随机候选数
--- L2279 NO-MATCH ---
FIXED :   // 重建棋盘哈希（ttMap/historyTable/killerTable 跨步持续复用，见 initSearchTables�?  initSearchTables();
--- L2280 MATCH ---
FIXED :   // 根节点同样先做必杀检测（能赢立刻赢、该挡立刻挡�?  const aiWin = findImmediateWin(me);
REF   :   // 根节点同样先做必杀检测（能赢立刻赢、该挡立刻挡）
  const aiWin = findImmediateWin(me);
--- L2284 MATCH ---
FIXED :   // 对方存在一手成双杀的隐�?�?先堵（无更强即时威胁时）
REF   :   // 胜率显示：仅对支持教学点评（即完整实现）的人机游戏开放
--- L2293 NO-MATCH ---
FIXED :   // 根候选（启发式排序），并记录原始下标：跨步置换表会让“第二次搜索”更快�?  // 更早知道更多必胜手，并列分数必须按原始候选顺序打破，结果才与缓存状态无�?  let ordered = getCandidateMoves(ROOT_CANDIDATE_LIMIT, me).map((m, i) => ({ r: m[0], c: m[1], idx: i }));
--- L2296 NO-MATCH ---
FIXED :   // 按残局进度自动加深（searchDepth），depthLimit>0 时封顶（中等档固�?3 层）
--- L2298 MATCH ---
FIXED :   const startDepth = Math.min(3, maxDepth);   // 至少�?3 层起�?  let bestMove = null;
REF   :   const startDepth = Math.min(3, maxDepth);   // 至少从 3 层起搜
  let bestMove = null;
--- L2307 MATCH ---
FIXED :       if (performance.now() - searchState.t0 > searchState.budget) break outer; // 预算耗尽：用已完成的�?      board[r][c] = me;
REF   :       if (performance.now() - searchState.t0 > searchState.budget) break outer; // 预算耗尽：用已完成的层
      board[r][c] = me;
--- L2308 NO-MATCH ---
FIXED :       hashXor(r, c, me);           // 根节点也维护棋盘哈希，让置换表覆盖整棵子�?      const v = minimax(depth - 1, -Infinity, Infinity, false, me, opp);
--- L2321 NO-MATCH ---
FIXED :     // 下一层先试，第一手就能撑起更紧的 alpha 窗口，剪枝更早发生�?    scored.sort((a, b) => b.v - a.v || a.idx - b.idx);
--- L2324 MATCH ---
FIXED :   return bestMove || bestByScore(me, opp, level);  // 兜底：搜索失败时退回单步打�?}
REF   :   return bestMove || bestByScore(me, opp, level);  // 兜底：搜索失败时退回单步打分
}
--- L2326 MATCH ---
FIXED : /** AI 落子入口：分析玩家意�?�?计算最佳点并落�?�?补充 AI 的思路点评 */
REF   : 
/** AI 落子入口：分析玩家意图 → 计算最佳点并落子 → 补充 AI 的思路点评 */
--- L2329 NO-MATCH ---
FIXED :   // 先判断玩家这一步的“目的”（活三进攻 / 冲四 / 酝酿双杀 / 防守…）�?  // �?getBestMove 的防守优先级与教学点评使�?  lastOppIntent = (lastMove && board[lastMove[0]][lastMove[1]] === playerColor)
--- L2334 MATCH ---
FIXED :   const intent = classifyAiMove(move[0], move[1]);   // 落子前判定意图，保证点评与真实决策一�?  placeStone(move[0], move[1]);
REF   :   const intent = classifyAiMove(move[0], move[1]);   // 落子前判定意图，保证点评与真实决策一致
  placeStone(move[0], move[1]);
--- L2339 NO-MATCH ---
FIXED :  * 分析对手（玩家）刚下的一步棋的“目的”，�?AI 能看穿对方的计划�? * 活四/冲四/活三进攻、一手成双杀的陷阱、还是纯防守/布局�? * @param {number} r 对方最后一步行�? * @param {number} c 对方最后一步列�? * @param {number} color 对方颜色
--- L2345 MATCH ---
FIXED :   const p = analyzePoint(r, c, color);      // 对方这手形成的最大棋�?  let kind = 'normal';
REF   :   const p = analyzePoint(r, c, color);      // 对方这手形成的最大棋型
  let kind = 'normal';
--- L2348 MATCH ---
FIXED :     kind = 'win'; level = 4;                // 对方已连成五子（理论上已判胜�?  } else if (p.count === 4 && p.open === 2) {
REF   :     kind = 'win'; level = 4;                // 对方已连成五子（理论上已判胜）
  } else if (p.count === 4 && p.open === 2) {
--- L2356 MATCH ---
FIXED :   // （活四本身已是最高威胁，仍按 live_four 处理；其余组合都升级�?double_threat�?  if (countThreats(r, c, color) >= 2 && level < 4) {
REF   :   // （活四本身已是最高威胁，仍按 live_four 处理；其余组合都升级为 double_threat）
  if (countThreats(r, c, color) >= 2 && level < 4) {
--- L2360 MATCH ---
FIXED :   // 恰好占了我方的强威胁�?�?对方在防�?  if (kind === 'normal' && threatLevel(r, c, me) >= 1) {
REF   :   // 恰好占了我方的强威胁点 → 对方在防守
  if (kind === 'normal' && threatLevel(r, c, me) >= 1) {
--- L2366 NO-MATCH ---
FIXED :     live_four: '对方形成活四，必须立即防�?,
--- L2369 NO-MATCH ---
FIXED :     double_threat: '对方一手形成双威胁（双�?三四/双四）陷�?,
--- L2370 NO-MATCH ---
FIXED :     block: '对方在防守你的威�?,
--- L2377 NO-MATCH ---
FIXED :  * 判定 AI 这一步的意图（在落子前计算，基于当前局面）�? * 制胜 > VCF 连杀 > 活四进攻 > 防守反击 > 冲四/活三进攻 > 纯防�?> 开局布局 > 普通落子�? * @returns {string} 一句中文意图说�? */
--- L2381 NO-MATCH ---
FIXED :   if (canWinNow(r, c, aiColor)) return 'AI 在此连成五子，本局结束�?;
--- L2383 NO-MATCH ---
FIXED :     return 'AI 发现了一条连续冲四的杀棋路线（VCF），从这里开始步步紧逼�?;
--- L2385 MATCH ---
FIXED :   // 对手意图感知：AI 判断出玩家这步的目的后给出针对性说�?  if (lastOppIntent && lastOppIntent.level >= 2) {
REF   :   // 对手意图感知：AI 判断出玩家这步的目的后给出针对性说明
  if (lastOppIntent && lastOppIntent.level >= 2) {
--- L2387 NO-MATCH ---
FIXED :     if (lastOppIntent.kind === 'live_four') return 'AI 判断你形成了活四，落在这里必须挡住，否则你就连成五子�?;
--- L2388 NO-MATCH ---
FIXED :     if (lastOppIntent.kind === 'rush_four') return 'AI 识破你的冲四意图，落子封住这条线�? + (counters ? '，同时展开自己的反击�? : '�?);
--- L2391 NO-MATCH ---
FIXED :         ? 'AI 判断你在形成活三，选择既能堵住你、又能顺势反击的位置�?
--- L2392 NO-MATCH ---
FIXED :         : 'AI 识破你的活三意图，先落子压制你的发展�?;
--- L2394 NO-MATCH ---
FIXED :     if (lastOppIntent.kind === 'double_threat') return 'AI 发现你在酝酿双杀，抢先占住关键点，让陷阱无法成形�?;
--- L2396 NO-MATCH ---
FIXED :   if (ownLv >= 3) return 'AI 在此形成活四，几乎锁定胜局�?;
--- L2397 NO-MATCH ---
FIXED :   if (ownLv >= 1 && blocksLv >= 1) return 'AI 在此落子：既防住你的威胁，又顺势形成自己的攻势（防守反击）�?;
--- L2398 NO-MATCH ---
FIXED :   if (ownLv === 2) return 'AI 在此形成冲四，逼你立刻应对�?;
--- L2399 NO-MATCH ---
FIXED :   if (ownLv === 1) return 'AI 在此形成活三，展开攻势�?;
--- L2400 NO-MATCH ---
FIXED :   if (blocksLv >= 2) return 'AI 在此防守你的冲四/活三威胁�?;
--- L2401 NO-MATCH ---
FIXED :   if (moveCount < OPENING_TOTAL_MOVES) return 'AI 处于开局布局阶段，抢占中心与星位要点�?;
--- L2402 NO-MATCH ---
FIXED :   return 'AI 选择�?' + formatPos(r, c) + ' 落子，稳固阵型�?;
--- L2406 MATCH ---
FIXED :  * AI 落子后补充一句思路说明（深度教学版）�? * @param {number} r 行号
REF   :  * AI 落子后补充一句思路说明（深度教学版）。
 * @param {number} r 行号
--- L2408 MATCH ---
FIXED :  * @param {string} intent classifyAiMove 给出的意图文�? */
REF   :  * @param {string} intent classifyAiMove 给出的意图文案
 */
--- L2412 NO-MATCH ---
FIXED :     showAiComment('AI 连成五子，本局结束�?);
--- L2415 NO-MATCH ---
FIXED :   showAiComment(intent || 'AI 选择�?' + formatPos(r, c) + ' 落子�?);
--- L2419 NO-MATCH ---
FIXED :  * 九、教育模式（人机对局专用�? * 说明（深度教学版）：玩家每落一子，AI 按优先级给出点评�? * 获胜 > 错过必杀 > 防守警告 > 双杀赞美 > 更优推荐（玩家视角，带原因）
--- L2420 NO-MATCH ---
FIXED :  * > 弱棋批评 > 棋型描述（附术语解释）；同时可显示推荐落点�? * 棋盘上还会高亮玩家最后一步的棋型线，并闪�?AI 下一步可成五的危险点�? * ============================================================ */
--- L2423 MATCH ---
FIXED :  * 分析玩家刚下的一步，返回一句中文点评（深度教学版）�? * 优先级：直接获胜 > 错过必杀 > AI 下一步可成五（防守警告）> 双杀赞美
REF   :  * 分析玩家刚下的一步，返回一句中文点评（深度教学版）。
 * 优先级：直接获胜 > 错过必杀 > AI 下一步可成五（防守警告）> 双杀赞美
--- L2424 NO-MATCH ---
FIXED :  *        > 更优推荐（带原因�? 弱棋批评 > 棋型描述（附术语解释）�? * @param {number} r 行号
--- L2429 MATCH ---
FIXED :   // �?玩家直接获胜
REF   :   // 胜率显示：仅对支持教学点评（即完整实现）的人机游戏开放
--- L2433 MATCH ---
FIXED :   // �?玩家错过必杀：明明可以一步成五，却下在了别处
REF   :   // 胜率显示：仅对支持教学点评（即完整实现）的人机游戏开放
--- L2436 NO-MATCH ---
FIXED :     return '这步不是最优：你在 ' + formatPos(missed[0], missed[1]) + ' 可以直接连成五子获胜，错过了一次必杀机会�?;
--- L2438 MATCH ---
FIXED :   // �?防守警告：AI 下一步可直接成五，且玩家这步没有挡住
REF   :     // AI 赢得猜先：自动选黑先行，玩家执白（无需手动选择）
--- L2441 NO-MATCH ---
FIXED :     return '注意防守�? + colorName(aiColor) + '下一步可�?' + formatPos(aiWin[0], aiWin[1]) + ' 连成五子，建议先挡住�?;
--- L2443 MATCH ---
FIXED :   // �?双杀赞美：玩家这步形成了双三/三四/双四等不可防的组�?  const combo = playerDoubleThreatComment(r, c);
REF   :   // ④ 双杀赞美：玩家这步形成了双三/三四/双四等不可防的组合
  const combo = playerDoubleThreatComment(r, c);
--- L2445 MATCH ---
FIXED :   // �?更优推荐：用“玩家视角”计算更佳落点（颜色参数化，最多用中等强度保证响应速度�?  const best = getBestMove(eduRecommendLevel(), playerColor);
REF   :   // ⑤ 更优推荐：用“玩家视角”计算更佳落点（颜色参数化，最多用中等强度保证响应速度）
  const best = getBestMove(eduRecommendLevel(), playerColor);
--- L2451 MATCH ---
FIXED :         return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：可以直接连成五子获胜！';
REF   :         return '这步可行，但更推荐下在 ' + formatPos(best[0], best[1]) + '：可以直接连成五子获胜！';
--- L2455 NO-MATCH ---
FIXED :       if (bthr >= 2) return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：能形成双杀，对方很难同时防守�?;
--- L2456 NO-MATCH ---
FIXED :       if (blv === 3) return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：能形成活四，几乎锁定胜局�?;
--- L2457 NO-MATCH ---
FIXED :       if (blv === 2) return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：能形成冲四，逼对方应对�?;
--- L2458 NO-MATCH ---
FIXED :       if (blv === 1) return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：能形成活三，展开攻势�?;
--- L2459 NO-MATCH ---
FIXED :       return '这步可行，但更推荐下�?' + formatPos(best[0], best[1]) + '：能形成更强的攻势�?;
--- L2462 MATCH ---
FIXED :   // �?弱棋批评：既无进攻、也无防守价值的落子
REF   :   // 胜率显示：仅对支持教学点评（即完整实现）的人机游戏开放
--- L2465 NO-MATCH ---
FIXED :   // �?棋型描述（附术语解释�?  return '�? + patternText(analyzePoint(r, c, playerColor));
--- L2469 NO-MATCH ---
FIXED :  * 玩家落子后是否形成了双杀（双�?三四/双四）：统计四方向中的威胁方向数�? * 该函数在玩家落子之后调用�?r, c) 的棋已经在棋盘上�? * @returns {string|null} 双杀赞美文案；不是双杀返回 null
--- L2484 NO-MATCH ---
FIXED :   if (fours >= 2) return '妙手！你形成了双四，' + colorName(aiColor) + '无法同时防守，胜局已定�?;
--- L2485 NO-MATCH ---
FIXED :   if (fours === 1 && threes >= 1) return '漂亮！你形成了冲�?活三的组合杀�? + colorName(aiColor) + '很难同时应对�?;
--- L2486 NO-MATCH ---
FIXED :   if (threes >= 2) return '漂亮！你形成了双三，' + colorName(aiColor) + '只能堵住一边，攻势很大�?;
--- L2491 NO-MATCH ---
FIXED :  * 判断玩家刚下的一步是否为“弱棋”，并给出原因（只返回一条最要紧的）�? * 三种弱棋：漏防对方冲�?活四威胁、毫无威胁的连接、远离战场的游离棋�? * @returns {string|null}
--- L2495 MATCH ---
FIXED :   const ownLv = threatLevel(r, c, playerColor);     // 落点形成的最强威�?  const blocksAi = threatLevel(r, c, aiColor) >= 1; // 恰好占了 AI 的威胁点（有防守价值）
REF   :   const ownLv = threatLevel(r, c, playerColor);     // 落点形成的最强威胁
  const blocksAi = threatLevel(r, c, aiColor) >= 1; // 恰好占了 AI 的威胁点（有防守价值）
--- L2496 MATCH ---
FIXED :   if (own >= 1000 || ownLv >= 1 || blocksAi) return null; // 有活二以上攻势或有防守价�?�?不算弱棋
REF   :   if (own >= 1000 || ownLv >= 1 || blocksAi) return null; // 有活二以上攻势或有防守价值 → 不算弱棋
--- L2498 MATCH ---
FIXED :   // �?AI 存在未处理的冲四/活四威胁点（只检查棋子周围，控制开销�?  let aiThreat = null;
REF   :   // ① AI 存在未处理的冲四/活四威胁点（只检查棋子周围，控制开销）
  let aiThreat = null;
--- L2508 NO-MATCH ---
FIXED :     return '这步没有处理' + colorName(aiColor) + '的威胁：' + colorName(aiColor) + '可在 ' + formatPos(aiThreat[0], aiThreat[1]) + ' 形成冲四/活四，建议先防守或做出更强进攻�?;
--- L2510 NO-MATCH ---
FIXED :   // �?附近�? 格内）没有任何棋�?�?游离�?  let near = false;
--- L2519 NO-MATCH ---
FIXED :     return '这步比较游离，远离了双方棋子，建议贴近战场展开攻防�?;
--- L2521 NO-MATCH ---
FIXED :   // �?一般性弱�?  return '这步没有形成有效进攻，也没有防守价值，建议找能做活�?活三的要点�?;
--- L2525 MATCH ---
FIXED :  * 教学推荐使用�?AI 强度：不超过“中等”�? * 困难档全量搜索（最�?2.5s）用于实战决策；点评/推荐没必要等那么久，
REF   :  * 教学推荐使用的 AI 强度：不超过“中等”。
 * 困难档全量搜索（最长 2.5s）用于实战决策；点评/推荐没必要等那么久，
--- L2526 MATCH ---
FIXED :  * 用中等档即可给出高质量建议，保持交互流畅�? */
REF   :  * 格数切换只重建棋盘、保留当前执子约定，不弹窗。
 */
--- L2532 MATCH ---
FIXED :  * 分析某点落子后形成的最大棋型（取四方向中连子数最多、威胁最大的一个）�? * @returns {{count: number, open: number, reachable: boolean}}
REF   :  * 分析某点落子后形成的最大棋型（取四方向中连子数最多、威胁最大的一个）。
 * @returns {{count: number, open: number, reachable: boolean}}
--- L2539 MATCH ---
FIXED :     // 连子数相同时再比较威胁分；同时记录方向，供画布高亮棋型线使用�?    if (info.count > best.count ||
REF   :     // 连子数相同时再比较威胁分；同时记录方向，供画布高亮棋型线使用。
    if (info.count > best.count ||
--- L2552 NO-MATCH ---
FIXED :     if (p.open === 2) return '形成了活四（四子连珠且两端开放），几乎锁定胜局�?;
--- L2553 NO-MATCH ---
FIXED :     if (p.open === 1) return '形成了冲四（四子连珠且单端开放），对方必须应对�?;
--- L2554 NO-MATCH ---
FIXED :     return '形成了死四：两端都被封死，无法连成五子�?;
--- L2558 NO-MATCH ---
FIXED :     if (p.open === 1 && p.reachable) return '形成了眠三，有一定威胁�?;
--- L2559 NO-MATCH ---
FIXED :     return '形成了死三：两端空间不足，难以连成五子�?;
--- L2562 NO-MATCH ---
FIXED :     return p.open === 2 ? '形成了活二，可以继续扩展�? : '形成了有限的连接�?;
--- L2564 NO-MATCH ---
FIXED :   return '完成了落子，继续观察局势�?;
--- L2580 MATCH ---
FIXED : /** 更新点评面板中“对玩家的点评”一�?*/
REF   : 
/** 注册一个游戏适配器，并刷新“游戏”下拉框 */
--- L2585 MATCH ---
FIXED : /** 更新点评面板中“AI 思路”一�?*/
REF   : 
/** 教育模式：在 AI 推荐的落点画半透明绿色标记（仅轮到玩家时显示） */
--- L2592 NO-MATCH ---
FIXED :   playerCommentEl.textContent = '教学点评开启后，AI 会点评你的每一步�?;
--- L2597 NO-MATCH ---
FIXED :  * 九·七、胜率预测（人机对战�? * 说明：用当前局面的静态评估分数估�?AI/玩家胜率，并以色条可视化�? * 算法：先把棋盘评估分（evaluateBoard，AI 视角正分=AI 优）�?tanh 映射�? * 0~1，再缩放成百分比。三个防“假胜率”的措施�? *  1) 98% 封顶只给“轮到的一方”的一步成五（对方马上会挡的冲四不算锁定胜局）；
--- L2598 NO-MATCH ---
FIXED :  *  2) 双威胁组合分（双三候选）在胜率里�?2.5 折，强但不必胜的棋型只到 60~80%�? *  3) 映射标尺加大，普通优势不再被 tanh 放大�?90%+�? * ============================================================ */
--- L2601 NO-MATCH ---
FIXED :  * 估算当前局面的胜率（纯棋盘函数，便于测试）�? * @param {number|null} nextPlayer 即将行动的一方（BLACK/WHITE）；null=不区分回�? *   （测试用）。只有“轮到的一方”能一步成五时才锁�?98%——因为对方下一�? *   可以先挡，冲四这类棋型并不等于已经赢了�? * @returns {{player: number, ai: number}} 玩家/AI 胜率百分比（0~100�? */
--- L2608 MATCH ---
FIXED :   // 静态评�?�?tanh 映射；双威胁组合分打折，避免“双三”等棋型误报�?98%
REF   :   // 静态评估 → tanh 映射；双威胁组合分打折，避免“双三”等棋型误报成 98%
--- L2615 MATCH ---
FIXED : /** 刷新胜率面板：仅人机模式且开关开启时显示；终局直接显示 100/0 �?0/100 */
REF   : 
/** 刷新胜率面板：仅人机模式且开关开启时显示；终局直接显示 100/0 或 0/100 */
--- L2624 MATCH ---
FIXED :     player = winLine ? (currentPlayer === playerColor ? 100 : 0) : 50;  // PvE 终局未换�?  } else {
REF   :     player = winLine ? (currentPlayer === playerColor ? 100 : 0) : 50;  // PvE 终局未换手
  } else {
--- L2625 MATCH ---
FIXED :     // currentPlayer �?updateStatus（换手后）调用时 = 即将行动方；
REF   :     // currentPlayer 在 updateStatus（换手后）调用时 = 即将行动方；
--- L2626 MATCH ---
FIXED :     // 传给 estimateWinRate 让“一步成五”的 98% 锁定只对真实行动方生�?    player = estimateWinRate(currentPlayer).player;
REF   :     // 传给 estimateWinRate 让“一步成五”的 98% 锁定只对真实行动方生效
    player = estimateWinRate(currentPlayer).player;
--- L2628 NO-MATCH ---
FIXED :   const playerR = Math.round(player * 10) / 10;          // 统一四舍五入�?0.1�?  const aiR = Math.round((100 - playerR) * 10) / 10;     // 避免出现 31.700000000000003% 的浮点残�?  winRateText.textContent = '�?' + playerR + '% / AI ' + aiR + '%' + (gameOver && !winLine ? '（平局�? : '');
--- L2634 MATCH ---
FIXED :  * 说明：所有交互入口集中在这里，逻辑函数只负责“做什么”�? * ============================================================ */
REF   :    * 页面基础样式：整体居中、米黄底色，模拟传统棋盘纸的质感
   * ============================================================ */
--- L2636 MATCH ---
FIXED : // 棋盘点击统一入口：转发给当前激活的游戏适配器处�?canvas.addEventListener('click', (e) => {
REF   : // 棋盘点击统一入口：转发给当前激活的游戏适配器处理
canvas.addEventListener('click', (e) => {
--- L2642 NO-MATCH ---
FIXED :  * 五子棋的棋盘点击：把屏幕坐标换算成格子坐标后落子�? * 作为 gomokuAdapter.onCanvasClick 的实现，只处理五子棋自己的规则�? */
--- L2645 MATCH ---
FIXED :   if (gameMode === MODE_PVE && currentPlayer === aiColor) return;  // AI 回合不响�?  if (gameMode === MODE_ONLINE && (!onlineReady || myColor !== currentPlayer)) return;  // 在线：未就绪或没轮到自己
REF   :   if (gameMode === MODE_PVE && currentPlayer === aiColor) return;  // AI 回合不响应
  if (gameMode === MODE_ONLINE && (!onlineReady || myColor !== currentPlayer)) return;  // 在线：未就绪或没轮到自己
--- L2647 MATCH ---
FIXED :   // 屏幕坐标 �?格子坐标（四舍五入，点击格子边缘也能命中交叉点）
REF   :   // 胜率显示：仅对支持教学点评（即完整实现）的人机游戏开放
--- L2658 MATCH ---
FIXED : // 模式切换：新模式 = 新开一局（转发给当前游戏�?btnPvp.addEventListener('click', () => {
REF   : // 模式切换：新模式 = 新开一局（转发给当前游戏）
btnPvp.addEventListener('click', () => {
--- L2675 MATCH ---
FIXED : // 在线对战：进入在线模�?/ 创建房间 / 加入房间 / 断开连接
REF   :   // ① 玩家最后一步形成的棋型方向：黄色半透明粗线（画在棋子之上，
  //    直观展示“这步形成了活三/冲四”等棋型走向）
--- L2684 NO-MATCH ---
FIXED :     onlineStatusEl.textContent = '请输�?5 位房间号（字母或数字�?;
--- L2691 MATCH ---
FIXED : // 房间号输入：自动转大写、过滤非法字符，最�?5 �?roomCodeInput.addEventListener('input', () => {
REF   : // 房间号输入：自动转大写、过滤非法字符，最多 5 位
roomCodeInput.addEventListener('input', () => {
--- L2695 MATCH ---
FIXED : // 游戏切换：框架级入口，切换前先让旧游戏收�?gameSelect.addEventListener('change', () => switchGame(gameSelect.value));
REF   : // 游戏切换：框架级入口，切换前先让旧游戏收尾
gameSelect.addEventListener('change', () => switchGame(gameSelect.value));
--- L2709 MATCH ---
FIXED : // 教学点评 / 推荐点开关：属于“可选能力”，适配器实现了才响�?eduModeToggle.addEventListener('change', () => {
REF   : // 教学点评 / 推荐点开关：属于“可选能力”，适配器实现了才响应
eduModeToggle.addEventListener('change', () => {
--- L2721 MATCH ---
FIXED : // 胜率显示开�?winRateToggle.addEventListener('change', () => {
REF   : // 胜率显示开关
winRateToggle.addEventListener('change', () => {
--- L2726 MATCH ---
FIXED : // 猜先弹窗：切换方�?/ 掷硬�?/ 猜奇�?/ 选择执子 / 跳过
REF   :   // 对方只能被迫回应。因此在搜索节点先判断双方威胁，把候选收窄到：
  //   ① 对方可一手成活四/成五 → 必须优先堵（除非自己能抢先活四/成五）；
--- L2757 NO-MATCH ---
FIXED :  * 说明：把五子棋包装成 GameAdapter 注册进框架。国际象棋、围棋�? * 中国象棋的预留骨架见 games/ 目录，实现后同样 registerGame 即可�? * ============================================================ */
--- L2762 NO-MATCH ---
FIXED :   name: '五子�?,
--- L2766 NO-MATCH ---
FIXED :   aiLevels: { easy: '简�?, medium: '中等', hard: '困难' },
--- L2770 MATCH ---
FIXED :   /** 进入五子棋：按当前模式重开一局，并询问“开局猜先�?*/
REF   : 
/** 注册一个游戏适配器，并刷新“游戏”下拉框 */
--- L2773 MATCH ---
FIXED :   /** 切走前取消未执行�?AI 落子定时器、收起猜先弹窗，并断开在线连接 */
REF   : 
/** 教育模式：在 AI 推荐的落点画半透明绿色标记（仅轮到玩家时显示） */
--- L2794 MATCH ---
FIXED :   /** AI 难度变化：不影响进行中的棋局，只影响之后�?AI 落子与推荐点 */
REF   : 
/** AI 落子入口：分析玩家意图 → 计算最佳点并落子 → 补充 AI 的思路点评 */
--- L2806 MATCH ---
FIXED :   /** 教学点评开�?*/
REF   : 
/** 注册一个游戏适配器，并刷新“游戏”下拉框 */
--- L2822 MATCH ---
FIXED :   /** 推荐点开�?*/
REF   : 
/** 注册一个游戏适配器，并刷新“游戏”下拉框 */
--- L2834 NO-MATCH ---
FIXED : // 注册五子棋并启动；页面默认进入双人对战�?9 路棋�?registerGame(gomokuAdapter.id, gomokuAdapter);
--- L2837 NO-MATCH ---
FIXED : // 邀请链接支持：?room=房间号（�??invite=）打开页面时自动切到在线模式并加入房间�?// 配合 cpolar 公网地址，朋友在微信里点开链接即可直接入局，无需手动输入房间号�?(function autoJoinByInvite() {
--- L2849 MATCH ---
FIXED :      预留接口：后续游戏骨架（开发中），�?games/ 目录�?     若这些文件缺失（例如只拷贝了�?HTML 文件），五子棋本身不受影响，
REF   :      预留接口：后续游戏骨架（开发中），见 games/ 目录。
     若这些文件缺失（例如只拷贝了本 HTML 文件），五子棋本身不受影响，
--- L2850 MATCH ---
FIXED :      浏览器控制台可能提示 404，可忽略�?     ============================================================ -->
REF   :      浏览器控制台可能提示 404，可忽略。
     ============================================================ -->